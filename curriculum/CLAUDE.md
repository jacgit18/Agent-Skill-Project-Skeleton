# CLAUDE.md — curriculum/

> **STUB — pending review.** This subtree (`curriculum/` + the related `.superpowers/sdd/`
> task briefs and reports) is a **separate spec-driven learning-portfolio project**, not
> part of the skill catalog. It has its own workflow: specs in `curriculum/specs/`, plans
> in `curriculum/plans/`, progress journals in `curriculum/journals/`.

The root `CLAUDE.md` conventions (skill-authoring workflow, `skill-interaction-testing`,
the README/backlog bookkeeping) **do not apply in here.**

Fill in the real workflow — how a spec becomes a plan becomes task briefs, where
`.superpowers/sdd/` fits, what "done" looks like for a project log — after reviewing the
existing files.
