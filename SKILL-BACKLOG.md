# Skill Backlog

Candidates found by reviewing `Architecture/` notes for skill-worthy material.
Last reviewed: 2026-09-05.

Process for each new skill: draft under the right group in `.claude/skills/` →
screen in isolation (baseline fails without it, skill fixes it) → run
`Prompts/skill-interaction-testing` against the existing set → record the result.
See [README.md](README.md) "Adding a skill".

---

## New skills — to build

### 1. `capacity-estimation` — priority 1 (strong)
- [x] Built `2026-09-04` — `.claude/skills/Architecture/capacity-estimation/` (SKILL + `estimation-method.md` + `worked-examples.md` + README). Isolation screen passed (baseline fabricates all 9 assumptions; skill gates then delivers). `skill-interaction-testing` run (~9 scenarios, 3 agents): no stacking/contradiction/starvation; the `technical-cost-decision` "assume vs refuse-to-invent" tension composes (usage drivers vs unit prices). Reciprocal edits applied: `technical-cost-decision` desc + "when a figure is missing", `learning-gate` Step 3 row + hand-off list + Never list, `data-tier-operations` & `resilience-strategy` out-of-scope pointers. Memory: `skill-interaction-capacity-estimation.md`.
- A-priori estimate from stated assumptions → QPS (avg + peak), bandwidth / egress GB, storage per day + per year + replicated, cache memory, server count, and **what binds first**.
- Gate: refuse to estimate until the user supplies DAU / traffic driver, actions per user, payload sizes, read:write ratio, peak:average ratio, growth horizon, replication factor. Stating the assumptions is the rep.
- Group: `Architecture/`
- Sources: [Bandwidth Estimation](Architecture/02.%20System%20Design/Bandwidth%20Estimation.md) · [Capacity Estimation](Architecture/02.%20System%20Design/Questions/Capacity%20Estimation.md) · [Music Streaming Service Estimation](Architecture/02.%20System%20Design/Questions/Music%20Streaming%20Service%20Estimation.md)
- Feeds: `technical-cost-decision` (dollars), `data-tier-operations` (is the DB the bottleneck), `resilience-strategy` (what binds first), `observability-strategy` (volume / cardinality)
- Boundary lines: NOT dollar cost (`technical-cost-decision`); NOT shard/replicate given the numbers (`data-tier-operations`); NOT a measured live bottleneck (`problem-solving-gates` Optimization / Rubber Duck). This is the estimate *before* telemetry exists.
- Caveat: the math in the source notes is self-flagged shaky (`#todo Double check calculations`) — clean up the method (walk order, unit/time tables, scoring) in the reference file.

### 2. `failure-mode-analysis` — priority 2 (strong)
- [x] Built `2026-09-04` — `.claude/skills/Architecture/failure-mode-analysis/` (SKILL + `nine-categories.md` + `scoring-and-register.md` + README). Procedure skill (modelled on `document-page-check`), not a gate. Isolation screen passed (baseline gives a narrative review, not a systematic nine-category walk / RPN register / watchlist / 3-way routing; skill delivers all of it). `skill-interaction-testing` (~9 scenarios, 3 agents): no contradiction / stacking / starvation; FMEA → resilience/observability/test-strategy handoffs are clean *from* FMEA, but FMEA was referenced by zero siblings. Reciprocal edits applied: `resilience-strategy`, `observability-strategy` (+ its "top failure modes" phrase), `test-strategy` (+ selection-framework Step 7) out-of-scope pointers; `learning-gate` Step 3 row + enumerations. Memory: `skill-interaction-failure-mode-analysis.md`.
- Structured FMEA / pre-mortem over a design or workflow: walk each component + interaction, enumerate failure modes by category (functional, availability, performance, consistency, integration, dependency, security, operational, human/process), record cause → manifestation → impact, score and rank → prioritized failure-mode register.
- Not a withhold-the-answer gate — a structured procedure skill (like `document-page-check`).
- Group: `Architecture/`
- Source: [Failure Modes](Architecture/02.%20System%20Design/Failure%20Modes.md) (+ chaos/fault-injection thread also appears in [Monitoring & Observability](Architecture/Monitoring%20%26%20Observability.md))
- Feeds: `resilience-strategy` (mitigations for dependency/overload modes), `observability-strategy` (alert on impact ranking), `test-strategy` (fault-injection / chaos target list)
- Boundary lines: this is **enumerate & prioritize all modes, proactively, whole design**; `resilience-strategy` is **pick the mechanism for a known overload/dependency failure**. `problem-solving-gates` Rubber Duck is one bug happening now. Needs reciprocal description edits on `resilience-strategy`, `observability-strategy`, `test-strategy`.
- Caveat: source note (`author: gitUserNamePlaceHolder`, no Status) explains *what* failure modes are but gives no repeatable procedure — the skill must add the walk order, the scoring rubric (RPN or lighter 2-axis), and the register format.

### 3. `design-scoping` — priority 3 (borderline, 3 consolidated sources)
- [x] Built `2026-09-04` — `.claude/skills/Architecture/design-scoping/` (SKILL + `scope-dimensions.md` + `significance-filter.md` + README). Gate; consolidates Specifying Scope indepth + Userbase + Define system threshold; folds in the LLD/HLD blast-radius classifier as the significance filter for deep-dive selection. Isolation screen passed (baseline sketches first on invented assumptions; `ambiguity-gate` does NOT catch a clear "design me a system" request — confirming the niche). `skill-interaction-testing` (~10 scenarios, 3 agents): no contradiction / starvation / same-turn stacking; all gates individually well-formed, risk was purely which door the agent enters. Reciprocal fixes: `ambiguity-gate` Step 1 row (established design intent → design-scoping — the backlog-anticipated clause), `ticket-evaluation` out-of-scope (bare system-name ticket → design-scoping), `microservices-decision` out-of-scope (greenfield/unscoped → design-scoping first), `capacity-estimation` out-of-scope (no-scope-yet → design-scoping first), `learning-gate` Step 3 row. Memory: `skill-interaction-design-scoping.md`.
- Front-door of the Architecture group. Refuse to design until the user has stated: purpose + audience; functional requirements + explicit out-of-scope; non-functional numeric targets (RPS ceiling, concurrency, latency budget, uptime, error budget, cost cap); constraints (team, timeline, existing stack, platforms, compliance); and the 1–2 features to design deeply.
- Output: a scope statement. Sequences into `capacity-estimation`, `microservices-decision`, `api-interface-style`, `database-architecture`, `failure-mode-analysis`.
- Group: `Architecture/`
- Sources (consolidate all three into ONE skill): [Specifying Scope indepth](Architecture/02.%20System%20Design/Specifying%20Scope%20indepth.md) · [Userbase](Architecture/02.%20System%20Design/Userbase.md) (user-base characterization + compliance checklist) · [Define system threshold](Architecture/Define%20system%20threshold.md) (non-functional numeric targets)
- Fold in — [Boundaries of LLD and HLD](Architecture/Boundaries%20of%20LLD%20and%20HLD.md): the blast-radius discriminator ("if I change this, how much breaks?" → whole system / data model = HLD · many modules = mid · one function = LLD), the *"who cares?"* test (architect / team lead / dev), and the "needs a migration plan / data rewrite / ops change → was never low-level" tell. Use it as the significance filter for the deep-dive-feature-selection step (which decisions deserve deep design + write-down). Port the classifier only — reversibility / expense-to-replace already lives in `technical-cost-decision`; don't duplicate the decision.
- Boundary lines: must defer to `ambiguity-gate` for "what do you even mean by this request" and take over only once "architect / design a system" is the established intent (precedent: `test-practice-gate` ↔ `ambiguity-gate`). Also a line vs `ticket-evaluation` (judges/sizes a defined ticket; this elaborates scope on an undefined ask).
- Caveat: highest absorption risk of the four — `ambiguity-gate` already claims "unstated scope". Descriptions must nail the system-design-specific split (functional / non-functional / scale numbers / deep-dive feature selection) that `ambiguity-gate` wouldn't know to ask.

### 4. `cloud-iam-boundary` — ad hoc, from a `Cloud/` folder review (not part of the Estimation/FMEA/Scoping batch)
- [x] Built `2026-09-04` — `.claude/skills/Architecture/cloud-iam-boundary/` (SKILL + `iam-mechanics.md` + `network-boundary.md` + README). Gate; decides who/what gets access to a cloud resource and its network placement. Corrected a real error in the source notes (a flat, incorrect IAM policy-evaluation order) into the actual nested-ceiling model. Isolation screen passed (baseline hands over or offers `AdministratorAccess` with only a caveat; skill refuses until the specific 3–5 actions are named). `skill-interaction-testing` (~10 scenarios shared with item 5, 3 agents, one relaunch needed after a worktree-snapshot timing issue): clean on every pair tested (`serverless-execution-model`, `resilience-strategy`, `design-scoping`, `security-review`). Reciprocal fixes: `design-scoping` out-of-scope + specialist sequence (both directions), `microservices-decision`/`data-tier-operations` untouched (no real overlap found), `learning-gate` Step 3 row. Memory: `skill-interaction-cloud-iam-and-serverless-execution.md`.
- Who/what gets access to a resource and where it sits on the network: principal, least-privilege permission set (specific actions/resources, never a wildcard), trust boundary + credential lifetime (STS-assumed role vs long-lived keys), permissions-boundary/SCP ceiling, public vs private subnet.
- Group: `Architecture/`
- Sources: `Architecture/02. Backing Service Options/Cloud/IAM Principles.md` · `Hierarchy of Policies in AWS.md` · `AWS/IAM Policies and Role Types in AWS.md` · `AWS/IAM Role Creation Process.md` · `AWS/Managing IAM.md` · `AWS IAM Monitoring, Auditing, & Automation.md` · `AWS/Server-less & Permission.md` · `AWS/serverless IAM Stuff.md` · `AWS/AWS Security Token Service.md` · `AWS/VPC & IGW.md` · `AWS/AWS Shield.md` · `Cloud Security Best Practices.md`
- Boundary lines: not the compute primitive (`serverless-execution-model`, which consumes the role this skill designs); not edge/DDoS defense (`resilience-strategy`); not encryption/secrets (unowned, name and defer); not code-level vulnerability scanning (`security-review`); defers to `design-scoping` for an unscoped system.

### 5. `serverless-execution-model` — ad hoc, from the same `Cloud/` folder review
- [x] Built `2026-09-04` — `.claude/skills/Architecture/serverless-execution-model/` (SKILL + `execution-model-decision.md` + `orchestration-and-failure-handling.md` + README). Gate; decides the compute primitive, invocation model, orchestration, and per-invocation failure contract for one unit of work. Corrected the source notes' absolute "never chain serverless functions" into the real, narrower caution (synchronous chaining specifically), and explained why Choice/Pass/Wait/Succeed/Fail states structurally can't carry Retry/Catch. Isolation screen passed (baseline wires the synchronous 3-Lambda chain roughly as asked; skill refuses and surfaces the idempotency/timeout-stacking risk). `skill-interaction-testing` (~10 scenarios, 3 agents, one relaunch needed after a worktree-snapshot timing issue) found 2 real gaps and fixed both: `technical-cost-decision` widened so "Lambda vs Fargate, which is cheaper" routes through this skill's fit gate regardless of phrasing; this skill's own bare "Lambda vs Fargate" trigger phrase was over-triggering-prone on a pure concept question, qualified + added a no-gate carve-out. Clean (no fix needed) against `microservices-decision`, `resilience-strategy`, `cloud-iam-boundary`, `deployment-strategy` (zero overlap, correctly silent), `data-tier-operations` (reciprocal pointer added anyway — real collision surface). Memory: `skill-interaction-cloud-iam-and-serverless-execution.md`.
- The compute primitive (FaaS / container task / long-running service), the invocation model (sync / async / poll-based event-source mapping), orchestration (state machine) vs choreography vs a single step, the messaging technology for a choreographed handoff (queue vs pub/sub vs stream/log vs ingest-to-destination), and the failure contract (Retry/Catch placement, DLQ/failure destination, the idempotency requirement retries impose).
- Group: `Architecture/`
- Sources: `Architecture/02. Backing Service Options/Cloud/Lambda vs Fargate.md` · `Microservices vs FaaS.md` · `Lambda Invocation Models.md` · `AWS/AWS Async Vs Sync.md` · `AWS/Step function.md` · `AWS/Step Function Extension.md` · `Step Function Catch Blocks.md` · `Failure States.md` · `AWS Parallel State.md` · `AWS/DLQ.md` · `AWS/DLQ Drainer.md` · `AWS/AWS SQS.md` · `AWS/Amazon Kinesis Data Streams vs Amazon Kinesis Data Firehose.md` · `Scalable Messaging Architecture.md` · `Qsink.md` (added 2026-09-04, second pass — see below). (`Lambda Invocation.md` in the same folder is about generic programming-language lambdas, not AWS Lambda — not used despite the name.)
- Second-pass fix `2026-09-04`: the original build (above) said it would fold the messaging-technology decision (SQS vs SNS vs Kinesis vs Kafka topology choice) into this skill but only covered the Lambda-consumption side; the actual queue/pub-sub/stream *choice* was missing. Added a "Choosing what carries an event between steps" section to `execution-model-decision.md` (delivery model / replay / ordering / multiple-readers decision table + decision rule), a gate-item-6 expansion, a "just use SQS for everything" challenge bullet, an output-block `Messaging technology:` line, and two new trigger phrases in the description.
- Boundary lines: not how many services exist (`microservices-decision`, which this skill assumes as given); not the execution role/network placement (`cloud-iam-boundary`); not overload/cascade retry-budget defense (`resilience-strategy` — composes with this skill's per-invocation contract); not dollar cost (`technical-cost-decision`); not rollout mechanism (`deployment-strategy`, zero overlap confirmed); not hot-partition/shard topology (`data-tier-operations`, only if the root cause is a genuinely hot key, not a poison-pill record).

### 6. `user-story-decomposition` — ad hoc, from a BSA-notes review (root-level notes + `Communication/Companies Worked At/TD/`)
- [x] Built `2026-09-05` — `.claude/skills/Business/user-story-decomposition/SKILL.md`. Not a gate over a whole system (that's `design-scoping`) — decides use-case vs. user-story format for one already-named epic/feature, then walks epic → user story → acceptance criteria, holding each story to an INVEST-style quality bar (cohesive/complete/consistent/correct/modifiable/unambiguous/feasible/testable) and a Definition-of-Ready checklist, with a MoSCoW pass for triaging several candidate stories at once. Isolation screen passed (baseline invents a persona and filler benefits, never raises the use-case-vs-story question despite a plausible failure path, smuggles an undefined term into acceptance criteria, gives no sizing or Definition-of-Ready check). `skill-interaction-testing` (8 scenarios, 3 agents): clean hand-off/chaining against `access-control-modeling` (one story set per actor, permission model itself handed off) and `technical-cost-decision` (independent trigger already chains cleanly on a stated volume); clean control (fully-specified opening assembled directly, no gate stacking). 4 real one-line gaps found and fixed: `design-scoping`'s Output section never chained its settled functional list forward to this skill (starvation risk) — added; `ticket-evaluation` only recognized bare *system* names for hand-off, not bare *feature* names with no actor/AC — added a second out-of-scope bullet; `ambiguity-gate`'s "design/architect/redesign" Step 1 row could misroute "write user stories for the X redesign" on the word "redesign" alone — added a backlog-artifact carve-out; `learning-gate` was missing a Step 3 row + both parenthetical skill lists — added. Memory: `skill-added-user-story-decomposition.md`.
- Group: `Business/` (single-file skill, no reference files — matches `ticket-evaluation`/`technical-cost-decision` convention).
- Sources: root-level `Process of gathering requirements.md`, `User Story Flow and Breakdown.md` (worked example only, folded into the skill's own condensed example — no separate skill-worthy content), `Use Case vs User Story.md`; plus `Communication/Companies Worked At/TD/User Stories.md` (the fuller Theme→Epic→Feature→User-Story ladder + splitting heuristic + use-case-vs-story reinforcement), `Acceptance criteria.md` (AC authoring, Definition of Ready, Definition of Done), `Attributes of Requirements & User Stories.md` (the INVEST-style quality attributes), `Type of Jira User Stories.md` (story-type taxonomy, background only), `Moscow prioritization.md` (folded in as the optional triage pass), and a stripped-down (non-TD-specific) version of `PI Planning.md`'s shirt-size sizing heuristic.
- Boundary lines: not scoping a system (`design-scoping`, upstream — its functional list is this skill's usual input); not judging an already-written ticket's sprint-worthiness (`ticket-evaluation`, downstream); not the technical design a story implies — schema/API/permission-model (the relevant Architecture specialist skill, named by name when a story surfaces one); not the elicitation conversation that produces the raw requirement (unowned — see loose thread below); not drawing UML diagrams (output is textual).
- Caveat: `Communication/Companies Worked At/TD/` turned out to hold much more BSA source material than the 3 files that prompted this skill — the rest was reviewed and deliberately NOT folded in here; see "No skill / no action" and "Loose threads" below for what was found and why it's a separate candidate, not scope creep on this skill.

### 7. `reliability-math` — ad hoc, from a single clipped article (`SRE math every engineer should know.md`)
- [x] Built `2026-09-05` — `.claude/skills/Architecture/reliability-math/` (SKILL + README, no separate reference file — the five rules are compact enough that a split would just move the same content). Procedure skill (like `failure-mode-analysis`/`document-page-check`), not a gate — forces the actual arithmetic (percentile check before trusting an average, `L = λ × W` computed on real numbers, SLO → minutes + burn-rate during an active incident, a utilization reading against the non-linear queueing-collapse curve, a 5-point graph-literacy checklist) instead of a qualitative discussion of the concepts. Isolation screen came back mixed rather than a clean pass: this run's baseline actually computed everything correctly on the test prompt, but the agent's own honest self-critique flagged that nothing forced the computation and a differently-primed pass could easily have stayed qualitative ("check your percentiles," "you're probably near capacity") — weaker evidence than prior isolation screens, noted rather than hidden. `skill-interaction-testing` (5 scenarios, 3 agents): clean vs `capacity-estimation` (a-priori sizing correctly wins on a from-scratch system despite Little's-Law-shaped numbers), clean vs `observability-strategy` (won't do SLO arithmetic on an SLO that doesn't exist yet), clean vs `problem-solving-gates` (stays silent once a hypothesis already exists), clean control. One real one-directional gap found and fixed: `resilience-strategy` never referenced this skill back despite the reverse pointer existing three times in this skill's own file — added a clause to `resilience-strategy`'s gate item 5 ("what binds first") plus a README sibling-diagram line. Also added a `learning-gate` Step 3 row + both parenthetical lists (same pattern as every prior new domain skill). Memory: `skill-added-reliability-math.md`.
- Group: `Architecture/` (procedure skill, not a gate).
- Source: `SRE math every engineer should know.md` (a clipped practical-guide article, not an Architecture/ note) — already had all the content; the skill's job was the forcing function (compute on the user's real numbers) plus the explicit boundary against four siblings that already use adjacent vocabulary.
- Boundary lines: not a-priori sizing for a system with no live traffic (`capacity-estimation`, whose `target_utilization ~0.6–0.7` this skill explains rather than re-derives); not deciding what to instrument (`observability-strategy`, whose SLIs/SLOs this skill's arithmetic consumes); not picking a protection mechanism once a pressure is quantified (`resilience-strategy`, which this skill's utilization/queueing reading feeds); not a debugging/optimization session that already has a hypothesis or a profile (`problem-solving-gates`, which this skill produces the *input* for, not a replacement of).

### 8. `problem-journal` — user-requested, not from a source-note review
- [x] Built `2026-09-05` — `.claude/skills/Prompts/problem-journal/` (single-file, no reference/README — matches the rest of `Prompts/` except `prompt-archive`). Two modes, added in two passes. **Journal** (first pass): retrospective procedure (not a gate) — after a coding problem is resolved, journals symptom/cause/fix, greps for an actual recurrence count, classifies recurring-pattern-vs-one-off and fundamental-concept-vs-environmental-fluke, and gives a worth-learning verdict that must name the count/classification behind it — modeled on `technical-cost-decision`'s "never assert a magnitude without arithmetic" applied to learning-worthiness instead of dollars. **Capture** (second pass, same day, user-requested follow-up): mechanical recording — the moment an error appears, whether resolved or not, save it verbatim as its own file in `Finance/Error Log/` using the vault's existing `Language Error.md` template, no judgment attached (same shape as `prompt-archive`'s Archive/Log split, applied to errors instead of prompts). Journal's recurrence check now greps both `Finance/Error Log/` (a much stronger signal — actual error text) and `prompt-archive`'s dated logs (weaker — only ever has what the user typed); the two modes cross-link (Journal updates a matching Capture file's Status to Resolved rather than leaving it stale). User raised the overlap with `learning-gate`/`problem-solving-gates` themselves when requesting the first pass; resolved by scoping Journal strictly to *after* a problem is resolved and handing the actual teaching back to those two skills rather than doing it inline.
- **Verification: two passes, two different outcomes — both now resolved.** First pass (Journal only): all 3 test agents failed outright on an API session rate limit (429) before producing a result; 3 fixes were applied by direct reasoning over the files instead (`learning-gate` Step 3 row + parenthetical lists, `problem-solving-gates` Rubber Duck note, `prompt-archive` disambiguation clause) and flagged as unverified. Second pass (adding Capture mode): rate limit had cleared — a real agent run (1 agent, 3 scenarios: Capture on an unresolved error, a 3-way "log" control across `prompt-archive`/Capture/Journal, Capture running alongside an active Rubber Duck session) came back CLEAN on all three, catching one real gap: Capture mode had no rule stopping a user's *unconfirmed* Rubber-Duck hypothesis from being written into the file as a stated root cause — the same diagnosis-leak Rubber Duck's gate exists to prevent, just moved into a written artifact instead of chat. Fixed: the Rubber-Duck out-of-scope bullet and the red-flags list both now require an unconfirmed hypothesis to be labeled explicitly unconfirmed, never asserted as settled.
- Group: `Prompts/`.
- Source: none for Journal mode (built from the user's own request and design discussion); `Finance/Error Log/Language Error.md` (an existing vault template, previously untracked) for Capture mode's file structure.
- Boundary lines: not live assistance-level classification (`learning-gate`); not forcing the hypothesis during active debugging (`problem-solving-gates` Rubber Duck — Capture mode can run alongside an active Rubber Duck session but must label any unconfirmed hypothesis as such, never as a stated cause); not archiving a reusable prompt or raw session log (`prompt-archive` — Journal reads its logs as one recurrence corpus, Capture writes an entirely separate artifact); not evaluating a prompt's performance (`prompt-tester`).

### 9. `change-surface-audit` — ad hoc, from `Architecture/Extra AI and decision points.md`'s "Feature changes" section
- [x] Built `2026-09-05` — `.claude/skills/Architecture/change-surface-audit/` (SKILL + `surface-checklist.md` + `removal-and-silent-changes.md` + README). Procedure skill (modelled on `failure-mode-analysis`/`document-page-check`), not a gate over one already-decided add/modify/remove change (or a "silent" change — dependency upgrade, config change, infra update): walks six blast-radius surfaces (API, data, state, performance, security, observability), classifies backward-compatible vs breaking, requires the expand-contract sequence for a breaking modify, and runs a hidden-dependents audit (API consumers, DB/ETL consumers, feature-flag debt, cached data/CDN, UI dead paths) before a removal. Most of the source file (Frontend/Backend deep breakdowns, Engineering SDLC decomposition, Cross-Cutting Concerns) was already fully owned by existing skills — confirmed by grep (feature flags split cleanly between `deployment-strategy`/`config-and-secrets-management`; auth/authz/observability/config/caching/resilience each already had a home) — only the "Feature changes" section was genuinely unowned decision-shaped material. Data governance (lineage/privacy/retention/classification), also flagged in the source, had only a bullet list with no procedure — logged as a future gap, not built (same treatment as IaC).
- Isolation screen **PASSED** (1 agent, worktree-isolated): scenario = growing `order.status` from 2 to 3 values. Baseline (no skill) treated it as purely additive — alter the type, wire the new branch, ship. It never flagged the exhaustive-match break elsewhere in the codebase, never proposed an expand-contract sequence, never named a point of no return. The skill's actual procedure caught all three, plus two bonuses the baseline had no mechanism for (a permissions check on the new value → `access-control-modeling`; dashboards filtering by status silently miscounting → observability).
- `skill-interaction-testing`: 1 agent, worktree-isolated, desk-checked against 6 siblings (Skill-tool registry didn't pick up the new skill mid-session, so live cross-invocation wasn't possible — verdicts are careful description-vs-description reads, not live multi-skill runs). Found 3 one-directional starvation risks (each sibling's disclaimer list omitted this new skill entirely) and 1 mild one; `resilience-strategy`, `relational-modeling`/`database-architecture`, and the built-in `code-review` were clean. Fixed: `failure-mode-analysis` (frontmatter + body out-of-scope list — one-change-reactive vs whole-design-proactive), `deployment-strategy` (frontmatter — this skill decides *whether* a change needs a compatibility phase, that skill decides *how* it's staged; both mention expand-contract, highest-risk pair), `migration-cutover` (frontmatter — one feature's removal vs. a whole system retiring, both use "point of no return"), `access-control-modeling` (frontmatter — mild, a compound change+permissions ask should audit the whole change first). Memory: `skill-added-change-surface-audit.md`.
- Group: `Architecture/`.
- Source: `Architecture/Extra AI and decision points.md` — "Feature changes" section only (Adding/Modifying/Removing features, the six per-surface risk checklists, the expand-contract worked example, the five hidden-removal-dependency categories, the "silent changes are the most dangerous" thread). The rest of the file was reviewed and NOT used — already covered elsewhere (see build note above).
- Boundary lines: not the diff's own correctness/cleanup (`code-review`/`security-review`, built-in); not proactive whole-design enumeration with nothing decided yet (`failure-mode-analysis`); not the rollout mechanism once a change is confirmed safe (`deployment-strategy`); not a live system-to-system migration (`migration-cutover`); not the protection mechanism for a surfaced overload/dependency risk (`resilience-strategy`); not the schema/table design itself (`relational-modeling`/`database-architecture`) or a scaling change (`data-tier-operations`); not the API versioning scheme (`api-interface-style`); not where a config value/secret lives (`config-and-secrets-management`); not the permissions model itself (`access-control-modeling`).
- Caveat: the interaction-test agent couldn't get the freshly-authored skill registered in its own Skill-tool call mid-session (worktree branched before the skill existed, and the tool registry doesn't rescan live) — so Part 2 is a careful desk-check against each sibling's actual frontmatter text, not a live multi-agent collision run. Worth a live re-run next time this skill's description changes, once the registry has picked it up naturally.

### 10. `seller-financing-evaluation` — user-requested, first skill in a new `Finance/` group
- [x] Built `2026-09-05` — `.claude/skills/Finance/seller-financing-evaluation/SKILL.md` (single-file). Found via a `Business Venture/` folder audit (~32 files: LLC/DBA/trademark formation, VC mechanics, acquisition-criteria checklists, 5 startup-funding-stage scenario files) run the same way as the `Communication/` audits — almost everything was reference/vocabulary/personal-narrative, no-action; `Seller Financing.md` was the one file that actually cleared the bar. User explicitly accepted the domain mismatch with the rest of this catalog (software-engineering/BSA) because they're about to expand into a `Finance/` skill group next.
- Forces the same "reasoning fails at the division, not at the concepts" discipline `technical-cost-decision` applies to engineering costs, here applied to a seller-financed business purchase: compute the amortized monthly payment (never trust a stated figure), benchmark down payment/rate/term against real market ranges (10-50% down, 5-10% interest, 3-7yr term), check it against the business's actual cash flow, and run the "7 Ds" seller-motivation diagnostic (Death/Disease/Dullness/Departure/Disagreement/Distress/Divorce) before any fairness verdict.
- **Isolation screen came back mixed, same pattern as `reliability-math`.** This run's baseline actually computed the amortization and a DSCR (debt-service-coverage ratio) correctly and grounded a specific verdict in it — but the agent's own self-critique named the "7 Ds" motivation read as the weak link: an ad hoc list of questions, not the named, structured framework with its risk/leverage mapping. That's exactly the gap this skill closes; recorded honestly rather than oversold as a clean baseline failure.
- `skill-interaction-testing` (2 agents, 4 scenarios): control (full Deal Surface run correctly, "partner disagreement" mapped to the **Disagreement** D specifically) clean; a vendor/product financing-plan scenario (superficially similar vocabulary — "financing," "down payment," "monthly payment") correctly stayed with `technical-cost-decision` and didn't cross-fire; a zero-terms-given scenario correctly asked one question instead of inventing a deal. No real gap found — one optional hardening applied anyway (a one-line vendor-financing carve-out in this skill's own description, since it costs nothing); a second suggested edit (broadening `ambiguity-gate`'s applicability wording) was NOT applied — no observed failure justified touching a shared, load-bearing skill on speculation.
- Group: `Finance/` (new).
- Source: `Business Venture/Seller Financing.md`.
- Boundary lines: not the broader "should I buy this business at all" decision (sourcing, non-financing due diligence, valuation — `Buying A Business.md`/`Market Deals.md`/`Starting a Business.md` were reviewed and are NOT folded in, too thin/conceptual on their own — logged as a possible future consolidation candidate if this skill's domain gets built out further); not a vendor/product financing plan (`technical-cost-decision` — no seller-as-lender relationship, no business changing hands); not legal-document drafting or a substitute for an attorney/accountant/appraiser.

### 11. `equity-trade-decision` — user-requested, second skill in `Finance/`
- [x] Built `2026-09-05` — `.claude/skills/Finance/equity-trade-decision/SKILL.md` (single-file, matches `seller-financing-evaluation`'s convention). Found via a full audit of `Finance/Investment/`, `Finance/Accounts/`, `Finance/Experiment/`, `Finance/Places/`, `Finance/Taxes/`, `Finance/Potential Project Ideas/` (27 files) run at the user's request, specifically flagging investment-strategy/framework candidates. User picked this one (of several candidates) to build first: a 3-stage equity-trade decision chain, merged into one skill rather than three, consolidating `Trading Order of Operations.md` (pre-trade checklist), `Sector Rotation.md` (cycle-stage → sector tilt framework), and `Risk management.md` (position-sizing math).
- Forces a pre-trade checklist (fundamentals/technical trend/relative performance/entry timing) actually answered before a share count; an economic-cycle-stage call backed by ≥2 named indicators, never asserted on vibes, before any sector tilt; and a position size computed as risk-budget ÷ per-share-risk — with the risk budget (money that could be lost) explicitly kept separate from the position cost (money actually spent). The source note (`Risk management.md`) itself conflated these two numbers in its own worked example (divided a $200 risk limit by a $12 share price to get "11 shares" — nonsensical; the correct risk-based calc off its own stated stop distance is 133 shares) — the skill's worked example corrects this rather than repeating the error, same pattern as the source-note corrections in `cloud-iam-boundary`/`serverless-execution-model`.
- **Isolation screen PASSED, cleanly** (not a "mixed" result like `reliability-math`/`seller-financing-evaluation`) — baseline answered "how many shares of a $12 stock with $10k" by dividing capital by price (833 shares), added stop-loss and diversification as advisory color *after* the number rather than as an input to it, and never separated risk from spend. This is exactly the failure mode the skill's own worked example warns against.
- `skill-interaction-testing` — first attempt used `isolation:worktree` and hit the known worktree-staleness failure (branched before the new file was committed, couldn't see it — same issue logged in `change-surface-audit`'s build note); retried non-isolated since interaction testing is read-only desk-checking with no mutation risk. 5 scenarios, clean on all: no collision with `seller-financing-evaluation` (shared word "stock" but no real ticker/entry/stop present in the seller-financing scenario, so this skill's trigger isn't met), `technical-cost-decision` (scoped to volume/bill/budget framing, doesn't misfire on a bare stock mention with no real trade numbers), `learning-gate` (bare conceptual "how does position sizing work" correctly stays there), `problem-solving-gates` Options Generator (scoped to software/architecture, not portfolio moves). One real gap found and fixed: `learning-gate` Step 3 table + Never-list parenthetical had no row/mention for this skill at all (same starvation pattern found on every prior new domain skill) — added.
- Group: `Finance/`.
- Sources: `Finance/Investment/Trading Order of Operations.md`, `Finance/Investment/Sector Rotation.md`, `Finance/Investment/Risk management.md`.
- Boundary lines: not options/day-trading/leveraged strategies (different risk math, out of scope — covers the aggressive options-income content found in `Finance/Experiment/Investment Strategy.md`, deliberately not folded in); not portfolio-level diversification-vs-concentration policy (`Finance/Investment/Diversification.md` — reviewed, explicitly out of scope, see tension note below); not a retirement-account or insurance-product decision (Backdoor Roth, Infinite Banking, death-benefit sizing — reviewed, separate future-skill territory, not folded in); not a bare conceptual question with no real trade (`learning-gate`).
- **A real tension found, not resolved by this skill:** `Diversification.md` argues for *concentration* ("avoid excessive diversification, benchmark to your best performer") while `Risk management.md` leans on ETFs specifically to cut volatility. This skill sizes one position against one risk tier and deliberately doesn't take a side on the portfolio-level question — logged here rather than silently picking one philosophy.
- **Rest of the `Finance/` audit — not built, logged for later:** `Backdoor Roth IRA.md` (Investment) and `Backdoor Roth.md` (Accounts) are near-duplicate content on the same mechanism (the Accounts one is the more polished 2026 rewrite) — genuine decision-shaped material (pro-rata rule, Form 8606) but needs the duplicate reconciled before a skill is drawn from it. `1099 Contractor Benifits.md` (rate-breakeven calc + entity choice) and `Signing a Lease.md` + `Apartment Questions.md` (pre-signing checklist + negotiation script) are also real decision-shaped candidates, not yet built. `High Level Budgey.md`/`Assets Rough Analysis.md` are duplicate personal budget content; `Finance Calulator.md`/`Finance_Calculator.md` are duplicate personal salary-calculator tools (typo'd vs. underscore filenames, the underscore one is the newer/better version); `Finance/Places/Places to Check Out.md` is a non-finance travel/restaurant bucket list, misplaced in this folder; `HSA & Previous Benifits.md` mixes real reference content with personal account/member-ID numbers sitting in plaintext.

### 12. `return-to-play-progression` — user-requested, first skill in a new `Health/` group

- [x] Built `2026-09-05` — `.claude/skills/Health/return-to-play-progression/SKILL.md` (single-file, matches `seller-financing-evaluation`/`equity-trade-decision`'s Finance-group convention — no reference files, no README). Found via a full `Health/` folder audit (Physical/MMA/BJJ/Striking/Recovery/Regular Gym/Swim, Food/Diet, Food/Drinking — ~55 files) run at the user's request, the same shape as the Finance/Business Venture audits: almost everything was personal workout programs, technique/vocabulary catalogs, recipes, and tactical sparring notes — reference material with no wrong-answer failure mode to gate. One file cleared the bar: `Physical/Recovery/Toe Rehab.md`'s criteria-based 4-phase return-to-activity progression (each phase gated on stated pain/ROM/function criteria, not a calendar guess, with an explicit "pain the next morning = tissue didn't tolerate the load, regress" rule and a named reinjury-risk failure mode for combat-sport athletes). Generalized beyond turf toe to any self-manageable musculoskeletal injury; folded in `Recovery Guidelines.md`'s smaller, complementary sharp-vs-dull-pain rule (nerve/bone → stop entirely, too aggressive → lighten) as one of the skill's red-flag distinctions rather than standing it up separately.
- Forces a criteria-based 4-phase gate (Protection → Early activation → Load the mechanism → Return to sport-specific force) — no phase advances on elapsed time alone, only on stated symptoms clearing a bar — plus an immediate-red-flag screen (deformity, total inability to bear weight, severe/worsening swelling, a felt "pop" with loss of function, numbness/pulse loss) that exits to a doctor before any phase-gating, and the next-morning-pain-vs-during-exercise-pain distinction most self-assessments skip.
- **Isolation screen PASSED**, cleanly (1 agent, non-isolated — scenario: "rolled my ankle 5 days ago in BJJ, swelling mostly gone, walking fine, can I squat and get back to light rolling this week?"). Baseline gave reasonable, safety-conscious advice but answered from invented assumptions (guessed ROM/tenderness/single-leg-stance were fine without asking), blurred squatting (generic loading) and rolling (sport-specific grappling pressure) into one soft "ease into both" recommendation instead of treating them as different phase gates, and mentioned next-morning pain only as a footnote for squats, never applied as a hard rule to rolling. The skill's own run separated the two gates explicitly, named live rolling as skipping Phase 3 (the most-rushed phase, most common reinjury point) if attempted at 5 days with unconfirmed single-leg stance, and applied the next-morning-pain rule uniformly.
- `skill-interaction-testing` (1 agent, non-isolated desk-run against the full candidate pool — no `Health/` siblings exist yet, so the pool was just the four cross-cutting skills: `ambiguity-gate`, `learning-gate`, `problem-solving-gates`, `problem-journal`): 4 scenarios, all clean. Control (bare injury question) → only this skill fires. Bare conceptual question ("how does return-to-play progression generally work") → answered directly, `learning-gate` doesn't misfire since its classifier and every worked example are software/engineering-specific with no health domain entry. Multi-turn gate release (missing inputs asked on turn 1, phase read given on turn 2 with no other skill re-firing or stacking). Pressure scenario ("just tell me yes or no") → the skill's own "Red flags — the walk isn't done" section already refuses a bare verdict with no criteria checked; `problem-solving-gates`' parallel "pressure doesn't change the gate" principle never engages (software-only trigger), so no real collision, just a parallel design choice. `problem-journal` and `problem-solving-gates` confirmed explicitly software-scoped by their own descriptions — no realistic collision surface with a physical injury. No fixes needed on any sibling. `learning-gate` Step 3 table row + the Step 3 hand-off parenthetical still added anyway, matching the standing practice of registering every new domain skill there even when the interaction test came back clean (discoverability for a future ambiguous case, not a fix for an observed failure). Memory: `skill-added-return-to-play-progression.md`.
- Group: `Health/` (new).
- Source: `Health/Physical/Recovery/Toe Rehab.md` (the 4-phase criteria-based progression model, the timeline-reality-check table, the "under-rehabbing" and sport-specific-force warnings) + `Health/Physical/Recovery/Recovery Guidelines.md` (the sharp-vs-dull / nerve-vs-aggressive-pressure pain distinction, folded in as one red-flag rule rather than a separate skill).
- Boundary lines: not a diagnosis — doesn't grade the injury or rule out a fracture/complete tear, routes immediate red flags (deformity, total inability to bear weight, severe/worsening swelling, numbness) to a doctor before any phase-gating; not a healthy athlete's training/warm-up/mobility programming (no injury, no gate, answered directly — this is why the rest of `Physical/`'s workout-program and technique-catalog files were left alone, see the "no skill / no action" note below); not a substitute for physical therapy (names when a real PT/doctor visit is warranted — a plateau past the typical window, a reinjury, a joint injury not clearly improving — rather than generating home phases indefinitely).
- **Rest of the `Health/` audit — reviewed, not built, personal reference material:** `Physical/Current Regimen.md`, `Gym Core Exercise Weight Goals.md`, `Personal Optimized Session.md`, `Regular Gym/Priority Exercises Graph Test.md` are this user's own workout program/weight-log data, not a generalizable decision procedure. `MMA/` (Grappling Priority Flows, Martial Arts Circling, Personal Hands, Submissions, Takedowns, the `BJJ Guards/` set, `Striking/`'s Defense Energy Management / Distance Management Tactics / Leg Kick Strategy / Strike Statistics / Striking Fluidity / etc.) are technique catalogs and tactical principles for real-time sparring decisions — reference knowledge a fighter draws on live, not a deliberative planning conversation with a wrong-answer failure mode an AI gate would catch. `Food/Diet.md`, `Food/Drinking.md`, and the `Recipes/` tree are recipes, brand lists, and calorie tables — no decision to gate. `Recovery/Cold Bath.md` and `Recovery/Bath Bomb Recipe.md` are personal ritual/recipe notes. `Swim/` is pure technique reference. None of these were folded in or flagged as loose threads — they're closer to the Finance-audit's "duplicate personal budget content" category than to a deferred skill candidate.

### 13. `index-tuning` — user-requested, not from a source-note review

- [x] Built `2026-09-05` — `.claude/skills/Architecture/Data/index-tuning/` (SKILL + `index-mechanics.md` + `audit-and-write-cost.md` + README). Procedure skill (modelled on `reliability-math` / `failure-mode-analysis` / `change-surface-audit`), not a gate — needs real inputs (engine + version, actual query columns, selectivity, write profile, existing index set, current `EXPLAIN` plan for a reactive case, hot-vs-rare per query), asks for what's missing rather than inventing it, then runs an 8-step walk: restate workload → derive access path per query → selectivity check → write-cost budget → redundancy/unused pass → rare-query escape → online build → re-measure. Covers the piece nothing owned: tuning/auditing indexes on a **deployed, populated** schema, as distinct from (a) `relational-modeling` step 7's first-cut index plan built from abstract access patterns during table design, and (b) `problem-solving-gates` Optimization's "is this even the bottleneck, go measure" gate — this skill runs *after* that measurement when the finding is index-shaped, and routes a non-index finding (disk sort, expensive SELECT-list function, stale stats) back.
- Origin: user asked (2026-09-05) whether any skill covered "indexing strategy — query performance becomes visible/predictable — read vs write vs storage cost — what's the actual query pattern (WHERE/JOIN columns), hot path or rare admin query". Answer was "partially — `relational-modeling` owns the design-time index plan, the reactive/audit discipline on a live schema is unowned and not on the backlog". User chose separate skill over folding into `relational-modeling` (different trigger, different inputs, different failure mode — the modeling gate's entities/relationships/lifecycle preconditions are friction for someone holding a slow query and a plan).
- Group: `Architecture/Data/`.
- Source: none — assembled from the standard b-tree indexing model (composite prefix rule, covering/`INCLUDE`, partial, expression, non-b-tree types, clustered-index implications) plus the write-cost/audit material (per-write index cost, redundancy detection, per-engine unused-index stats queries, the rare-query playbook, per-engine online-build catch-list, Postgres bloat) and the boundary work against the four bordering skills.
- Boundary lines: not the first-cut index plan during schema design (`relational-modeling` step 7); not the measurement gate (`problem-solving-gates` Optimization — runs before this); not a non-index bottleneck the plan reveals (back to `problem-solving-gates` Optimization); not partitioning/sharding/replicas/shard-key or analytical-warehouse physical tuning — sort/dist/clustering keys (`data-tier-operations`, with `dimensional-modeling` for the model); not keeping a cacheable hot read off the DB (`caching-strategy`); not the dollar cost of extra storage/IOPS/a replica (`technical-cost-decision`); a `DROP INDEX` is a removal → `change-surface-audit` hidden-dependents lens; staging the index migration → `deployment-strategy` (expand/contract, invisible-index soak).
- Reciprocal edits applied (pre-verification): `relational-modeling` (SKILL desc + When-to-use bullet + Out-of-scope bullet + `indexing-and-constraints.md` Index plan note + README), `data-tier-operations` (SKILL Out-of-scope bullet + `operations-framework.md` step 2 + README defers-to list), `problem-solving-gates` (Mode 4 Optimization hand-off bullet), `learning-gate` (Step 3 table row + Database-design row's defer-to list + the don't-stack paragraph + the Never-list parenthetical), root `README.md` (Data table + pipeline diagram).
- **Verification done `2026-09-05` (2 agents, non-isolated — new files uncommitted, so worktree isolation would be stale; same call as `change-surface-audit` / `equity-trade-decision`).**
  - **Isolation screen: MIXED** (same as `reliability-math` / `seller-financing-evaluation`). Scenario: a grown 30M-row Postgres `orders` table, the equality-predicates-plus-sort-plus-`LIMIT` query, full cardinalities + write profile given, no `EXPLAIN` supplied. Baseline (no skill) was already a strong, near-complete answer on this textbook pattern — got composite column order with selectivity reasoning, the partial-index alternative, argued *against* `INCLUDE` on write-rate grounds, spotted the redundant `(customer_id)` prefix, `CONCURRENTLY` + `INVALID`-index caveat, re-measure. None of the 7 target gaps was outright missed. Skill's real adds: (1) write-cost budget as **actual arithmetic** (index count, ~67 inserts/s, per-insert/per-update delta, net-zero-after-drop) — the one clear outperformance; (2) selectivity check and hot-vs-rare as **named checkpoints with output-block lines** rather than lucky freehand; (3) safer drop procedure (`change-surface-audit` + drop-after-verify). Agent's own note: verdict is sensitive to baseline strength — would lean PASS against a median answer or on a messier case (a low-cardinality leading column someone actually proposed, a 14-index audit, a monthly-report query where the rare-query playbook does real work). Recorded honestly, not oversold.
  - **`skill-interaction-testing`: CLEAN**, 7 scenarios, 1 hardening fix. No Stacking / Contradiction / Starvation manifested. The 4 pre-applied reciprocal edits all verified as genuinely closing their gaps: `relational-modeling` deployed-side (S1, no schema re-model demanded) + design-time control (S2, `index-tuning` stays out); `problem-solving-gates` Optimization two-turn (S3 — turn 1 gate holds, `index-tuning` doesn't guess; turn 2 measurement is index-shaped → clean hand-off, no gate re-fire, no re-demand of the measurement); `learning-gate` conceptual control (S4 — no false trigger on "how do indexes work / covering vs partial", keyword surface suppressed by the "existing, populated database" framing); `data-tier-operations` (S5 — leads on the sharding pressure-test, hands a specific slow query down via framework step 2, `index-tuning` never claims the topology decision). S7 `change-surface-audit` index-drop chain clean (audit *which* are unused → audit *what the drop breaks*, complementary not duplicative). **One fix applied:** `caching-strategy`'s "query or index tuning of one slow query" hand-off (description + Out-of-scope bullet) still named only `relational-modeling` / `problem-solving-gates` — `index-tuning` → `caching-strategy` was wired but the reverse was stale (two-hop self-heal via the S1 carve-out, minor friction, not hard Starvation). Added `index-tuning` to both. `ambiguity-gate` not stress-tested (no vague-verb scenario); `dimensional-modeling` / `database-architecture` correctly dormant (no OLAP prompt, persistence settled).
  - Pairs recorded tested-and-clean (`index-tuning` ×): `relational-modeling`, `problem-solving-gates` (Optimization), `learning-gate`, `data-tier-operations`, `caching-strategy` (after the pointer fix), `change-surface-audit`. Memory: `skill-added-index-tuning.md`.

### 14. `model-routing-decision` — from `Priority Dev Roadmap.md`, first skill in a new `AI Engineering/` group

- [x] Built `2026-09-05` — `.claude/skills/AI Engineering/model-routing-decision/SKILL.md` (single-file, matches Finance/Health-group convention). Origin: reviewing the just-re-added-to-root `Priority Dev Roadmap.md` (a personal career roadmap, mostly reference/goal lists with no decision to gate — cert ladders, coding-challenge metrics, learning-priority tables) turned up one genuinely decision-shaped section: "Model routing as an engineering pattern," a worked framework for tiering LLM calls by task type plus a 7-way disambiguation of what people mean by "model routing" (only 4 of the 7 are actually model-selection; the rest are failover, ops triage, or multi-agent labor division mislabeled the same way). Nothing in the existing catalog owns an "AI/LLM engineering" domain — `Architecture/` is system-design generally, not LLM-specific — so this opens a new group rather than folding in anywhere.
- Places the request in the 7-way table first (refuses to build a routing architecture for a problem that's actually failover or ops-triage), then gates on task-type categories, volume, latency, cost sensitivity, and auditability before recommending build-your-own-classifier vs. a proxy, forces the actual pipeline (classifier model, lookup table, fallback tier, outcome logging) if build-your-own, and hands pricing off to `technical-cost-decision` rather than computing it.
- **Isolation screen (2 scenarios, 1 agent, non-isolated — file uncommitted): PASS / MIXED.** Scenario A (5,000 tickets/day, bill getting expensive, genuine tiering question) — PASS: baseline reliably skipped stating a latency budget or auditability need, skipped a fallback for classifier failure, and asserted savings with no arithmetic; the skill's Step 2/4/5 catch all three concretely. Scenario B (Bedrock calls timing out under load, asked as "should I route to a different model") — MIXED: a capable baseline already correctly reads this as failover, not cost-tiering, so the skill's Step 1 row 5 formalizes a correct call rather than fixing an error — recorded honestly rather than oversold, same pattern as `reliability-math` / `seller-financing-evaluation` / `index-tuning`.
- **`skill-interaction-testing` (1 agent, non-isolated desk-check, 5 scenarios vs `technical-cost-decision` / `capacity-estimation` / `problem-solving-gates` / `learning-gate`): clean, 1 real gap found and fixed.** `capacity-estimation` clean (this skill's own Step 2 owns routing-specific inputs; `capacity-estimation` invoked only when overall system traffic isn't sized yet, per this skill's own text). `problem-solving-gates` clean (a bare "LangChain or CrewAI" framework question is explicitly pre-empted in this skill's own description as not-a-trigger). Multi-agent re-entry scenario clean by design (Step 1's own text anticipates "once sub-agents exist, this skill reapplies to each one's model choice"). One real gap: `technical-cost-decision`'s hand-off was one-directional — no pointer back, so landing there directly with "what would tiered Claude/GPT calls cost" gave no prompt to check whether the tiering architecture was decided yet. Fixed: reciprocal pointer added to `technical-cost-decision`'s description. One soft ambiguity (not a hard gap): `learning-gate`'s bare "what is model routing, conceptually" scenario didn't over-fire on a literal reading of Step 2's gate condition, but the exemption was implicit — hardened anyway with an explicit sentence in this skill's own Step 2. `learning-gate` Step 3 table row + both parenthetical lists (Step 3 hand-off list, Never-list) added per standing practice. Memory: `skill-added-model-routing-decision.md`.
- Group: `AI Engineering/` (new).
- Source: `Priority Dev Roadmap.md` (root-level personal roadmap note) — "Model routing as an engineering pattern" section only. The rest of the file (Skills Focus, Fundamental Revisit, Cloud Focus checklist, Personal Metrics, cert ladder, the Engineering/Thin-Middle/Science-layer learning-priority tables) is personal reference/goal material with no wrong-answer failure mode — logged as no skill / no action, same bucket as `Agile.md` / `PI Planning.md`.
- Boundary lines: not pricing the tiers once volume/tokens are known (`technical-cost-decision`, which this skill hands off to); not sizing the overall request volume a routing decision sits inside (`capacity-estimation`); not a retry/fallback-on-error pattern across models or regions (a reliability/availability concern, not this skill, even though it's frequently called "routing" too); not a bare architecture-framework question with no per-call model-tier decision (`problem-solving-gates` Options Generator); not which sub-agent an orchestrator delegates a subtask to (an agent-architecture/labor-division decision this skill doesn't own — it re-enters only once that structure exists and a per-sub-agent model choice is the actual question).
- **Not built from this same review, logged for later:** the `Priority Dev Roadmap.md` "Agent to Consider: System Map Agent" idea (a continuously-maintained live dependency graph + NL query layer answering "what's affected if I change X") isn't skill-shaped — it's a standing tool to build, not a decision procedure Claude walks in conversation — and is already substantially covered manually by `change-surface-audit`'s blast-radius walk. The "have a conversation with AI, get to a spec, then give precision instructions" thread (same note, "so have conversations with AI before building anything...") was flagged here as a candidate second `AI Engineering/` skill — built next as item 15, but placed in `Skill Development/` instead (see below; the domain call changed once the actual shape was drafted).

### 15. `spec-drift-gate` — from the same `Priority Dev Roadmap.md` review, item 14's deferred thread

- [x] Built `2026-09-05` — `.claude/skills/Skill Development/spec-drift-gate/SKILL.md` (single-file). Source: the same note's "so have conversations with AI before building anything... problem framing, then controlled experiments... once you get clarity then you go into spec development applying surgically and after that you focus in precision instruction" thread — logged in item 14 as a candidate `AI Engineering/` skill, **but placed in `Skill Development/` instead**, next to `learning-gate` / `problem-solving-gates`: it governs the discipline of running *any* AI-assisted build without drift (not specific to designing an LLM-powered system, which is what `AI Engineering/` actually turned out to mean once `model-routing-decision` existed to compare against).
- Refuses to start a substantial multi-file/multi-session build until a written spec exists (problem framing, tradeoffs actually weighed — not asserted post-hoc, explicit in-scope *and* out-of-scope, an optional controlled-experiment slice for uncertain approaches), then at later checkpoints (new phase, session resuming, a proposed action touching something the spec never mentioned) diffs the work against that spec and forces an explicit amend-or-pull-back decision instead of letting scope silently expand.
- **Isolation screen (3 scenarios, 1 agent, non-isolated): PASS on all three.** A (multi-session notification-system build, turn 1) — baseline jumps straight into building the email piece with no stated tradeoff or out-of-scope line; skill's Step 2 forces both plus a possible controlled-experiment slice. B (mid-build drift — "while you're in the email service, wire up the promotional templates too, infra's already there," against a spec that explicitly scoped promotional out) — baseline just does it (classic silent scope creep); skill's Step 4 catches the never-mentioned action and forces an explicit amend-or-pull-back call. C (one-shot "add a null check before user.email access") — correctly inert, Step 1's first row exits immediately, no spec manufactured for a fully-specified single edit.
- **`skill-interaction-testing` (1 agent, non-isolated desk-check, 7 scenarios vs `learning-gate` / `problem-solving-gates` / `ambiguity-gate` / `design-scoping` / `change-surface-audit` / `session-handoff`): 2 real gaps found and fixed, rest clean.** `learning-gate`/`problem-solving-gates` clean, no stacking (build requests exit at learning-gate's Execution-intent classification immediately; problem-solving-gates explicitly excludes "write new code from scratch" — this skill is the sole gate on build requests, confirmed no double-gating, so no Step 3 row was added to `learning-gate` — its Step 3 table is specifically about deferring a *learning* rep, and build-intent already exits before reaching it, unlike every prior new domain skill added there). `ambiguity-gate` clean by design (its own description already defers reading-resolution first; this skill's own description only engages once "build this multi-step thing" is the confirmed reading — no reciprocal pointer needed, each self-triggers off its own text). `change-surface-audit` clean (orthogonal: this skill says "in scope, proceed," that skill still separately catches blast-radius risk on an in-scope change — being in-scope doesn't suppress the audit). **Gap 1 (fixed):** `design-scoping` hand-off row in this skill's own Step 1 table said "move to Step 3" outright once a scope statement exists, silently skipping Step 2 items 2 and 4 (tradeoffs, controlled-experiment slice) that the skill's own description promises still apply — the procedure contradicted its own description. Fixed: the row now names items 1/3 as satisfied by `design-scoping` and items 2/4 as still required. **Gap 2 (fixed):** `session-handoff` had zero awareness of a spec-drift-gate artifact — its "What to gather" step would re-derive Goal / Decisions & Constraints from the conversation, risking a duplicate copy that drifts from the spec. Fixed: added a line to link to an existing spec-drift-gate spec for those two items rather than re-deriving them.
- Group: `Skill Development/` (placement corrected from item 14's "candidate `AI Engineering/` skill" guess — see build note above).
- Source: `Priority Dev Roadmap.md`, the spec-driven-development thread only (already fully attributed under item 14; nothing further pulled from the note).
- Boundary lines: not a single fully-specified one-shot request (nothing to gate — the request already is the spec); not system-design's purpose/audience/functional/non-functional numeric targets (`design-scoping`'s front door — this skill treats a settled scope statement as satisfying two of its own four Step 2 items, not all four); not resolving which of several readings a request has (`ambiguity-gate` — resolves first, this skill's gate applies only once intent is settled as a multi-step build); not auditing an already-decided change's blast radius against the existing codebase's dependents (`change-surface-audit` — audits the build against its own stated plan over time, a different axis, composes rather than substitutes); not the end-of-session context dump (`session-handoff` — this skill's spec is what a handoff should point back to, not a replacement for writing one).
- Memory: `skill-added-spec-drift-gate.md`.
- **Extension `2026-09-05` — actual agent infrastructure, not just a skill.** Added Step 3a (execution handoff) plus two `spec-executor`-aware Red flags, and a new `.claude/agents/spec-executor.md` subagent definition — the piece that makes this skill's spec an input to something that actually runs unattended, not just a document the foreground conversation refers back to. `spec-executor` is briefed with exactly the spec + the current slice's precision instruction + a worktree, executes, commits in the worktree, never merges/pushes, and reports what it did plus what it flagged as outside the spec. Its report is explicitly treated as a Step 4 checkpoint, never a self-certifying approval — the skill's own Step 4 now says so directly. **Not re-run through a full isolation/interaction cycle** — this is additive to an already-verified gate (Steps 1-2 and the core precondition are unchanged) and doesn't introduce a new entry point that bypasses them, validated by inspection rather than a fresh empirical test; worth a live check the first time a real slice actually gets handed to `spec-executor`, rather than assumed clean indefinitely on design reasoning alone. Also documented in a new `README.md` "Agents" section, alongside the sibling cloud routine below.

### 15a. Weekly Catalog Drift Audit — cloud routine, not a skill

- [x] Created `2026-09-05` via `/schedule` — a recurring cloud routine (`claude.ai/code/routines`, trigger id not tracked here since routines aren't part of this repo's own catalog) that runs `catalog-drift-audit`'s procedure against `main` every Monday 9am America/New_York. Not a `.claude/skills/` entry — logged here only because it operationalizes item 16 into something that actually recurs on a calendar, closing the loop the "supports long-term things" conversation asked for.
- Behavior: reads `.claude/_Prompts/catalog-audit-log.md` first (its own durable trail, avoids re-flagging resolved items), runs Steps 1-3 and 5 of `catalog-drift-audit` for real (Step 4's untested-pair backfill is explicitly skipped in the unattended run — no live multi-agent testing from a cloud sandbox), fixes mechanical findings on a `catalog-audit/<date>` branch, and opens a PR rather than pushing to `main` directly (explicit user choice — PR-with-review over full autonomy). Appends a dated summary to the audit log every run, including clean runs.
- **Known gap at creation time:** none of today's session's work (`index-tuning` through `catalog-drift-audit` itself, plus the README/backlog reconciliation) was pushed to `origin/main` yet when this routine was created — its first fire will find `catalog-drift-audit/SKILL.md` missing from `main` and is instructed to stop and report that plainly rather than improvise. Resolves itself once the user pushes; not a bug in the routine, a sequencing note for whoever reads this entry later wondering why the first run did nothing.
- Not verified via isolation/interaction screening — it's a cloud cron job wrapping an already-verified skill's procedure, not a new skill in this catalog.

### 16. `catalog-drift-audit` — from the "supports long-term things" reframe of the long-horizon-agent conversation

- [x] Built `2026-09-05` — `.claude/skills/Prompts/catalog-drift-audit/SKILL.md` (single-file). Origin: after `spec-drift-gate` shipped, the user clarified that "long horizon" as they'd originally meant it was closer to *ongoing/perpetual support*, not just a single bounded build spanning sessions — pointing at a real gap: `skill-interaction-testing` only ever checks a *new* skill against a scoped pool; nothing re-checks the whole existing catalog as it rots over calendar time (stale docs, dead pointers, skills nobody references).
- Five-step procedure: (1) stale-marker check — `SKILL-BACKLOG.md`'s "Memory: pending" markers cross-checked against the memory directory; (2) catalog-doc sync — every skill on disk cross-checked against `README.md`'s tables; (3) dead-reference spot check on the fixed cross-cutting gate names; (4) untested-pair backfill — old skill pairs that were never tested against each other because neither was "new" at the same time, handed to `skill-interaction-testing`'s own method rather than re-deriving it; (5) starvation-by-neglect — skills no sibling's description ever points to. Not a gate (nothing to withhold) — a procedure that emits a report and applies the mechanical fixes directly.
- **Isolation screen: run for real against the live repo, not hypothetically — PASS, and the findings were real, not constructed.** An agent ran an unguided baseline skim of `SKILL-BACKLOG.md`/`README.md` first, then the actual Steps 1-3, on this repo's real state. Step 1 found all 4 existing "Memory: pending" markers were stale (the memory file existed for every one — `user-story-decomposition`, `reliability-math`, `change-surface-audit`, `return-to-play-progression` — zero were genuinely still pending), plus a 5th flavor of the same drift (`model-routing-decision` had no Memory line at all despite its file existing). Step 2 found 8 built skills missing a `README.md` row (`access-control-modeling`, `bff-gateway-placement`, `change-surface-audit`, `config-and-secrets-management`, `service-mesh-adoption`, `equity-trade-decision`, `software-carpentier-brand`, plus the entire `Health/` group). Step 3 (dead-reference spot check) came back clean. Baseline skim caught the general shape but, by the agent's own account, would have caught at most 2-3 of the 8 missing README rows and had no way to distinguish stale-vs-genuinely-pending without the structured memory cross-check — the procedure concretely outperformed. All mechanical fixes (4 stale markers + the missing model-routing-decision line, all 8 README rows) applied directly per this skill's own Step 6.
- **Bonus finding — since resolved (`2026-09-06`):** the audit surfaced a pre-existing, unrelated "deep research engine" skill (`Architecture/reddit-researcher/`, plus an untracked copy under `.claude/skills/_Other/`). Triaged: rewritten to drop the paid Apify dependency (now three free sources — Reddit public JSON, Algolia HN, native web), relocated to `Research/reddit-researcher/` as its own group, and the `Architecture/` copy deleted. Memory: `skill-folds-other-directory.md`.
- **`skill-interaction-testing` (division-of-labor check, not a full collision run — this skill doesn't fire on domain requests the way other skills do): clean.** Confirmed both skills' own descriptions already state the split correctly (per-new-skill scoped pool vs. periodic whole-catalog pass), that a "I built 3 skills today, check everything's okay" request correctly chains rather than collides (`catalog-drift-audit` runs first per its own "after a batch of skill work" trigger, Step 4 backfills any of-the-3-vs-each-other pairs), and that neither `ambiguity-gate` nor `learning-gate` false-triggers on an audit request. **One real gap found and fixed:** `skill-interaction-testing` had no reciprocal pointer to this skill (the same one-directional-pointer pattern fixed repeatedly throughout this backlog) — added.
- No `learning-gate` Step 3 row added, matching `skill-interaction-testing`'s own precedent — confirmed by grep that `skill-interaction-testing` itself has zero presence in `learning-gate`, since both are developer-facing catalog-authoring tools, not domain skills an end-user's request would route through learning-gate's intent classifier.
- Group: `Prompts/` (sibling of `skill-interaction-testing`).
- Source: none from a note — built directly from a real, observed gap in this project's own process, verified against this repo's actual live state rather than a synthetic scenario.
- Boundary lines: not a substitute for `skill-interaction-testing` on a new/changed skill (still mandatory per-skill, this doesn't replace it — it catches what accumulates *between* those runs); not a rewrite pass (findings here are one-line reconciliations, not a reason to restructure a skill's content); not an automated cadence — no cron/scheduler is wired into this repo, so this skill is the procedure a `/schedule` or `/loop` job would run, not the scheduler itself.
- Memory: `skill-added-catalog-drift-audit.md`.

### 17. `system-design-communication` — converted from a copied-in, non-functional `.claude/agents/` directory

- [x] Built `2026-09-05` — `.claude/skills/Business/system-design-communication/` (SKILL + `explain-design.md` + `interview-simulation.md` + README). Origin: user had copied a `.claude/agents/system-design-communication/` directory in from another project (`AGENTS.md` + `README.md` + `prompts/{explain-design,interview-simulation}.md`) and asked whether to change it or drop it. Diagnosis: structurally inert in this harness — Claude Code subagents load from a single flat `.claude/agents/<name>.md` file (confirmed by contrast with `spec-executor.md`, which *did* get picked up as an available agent type the moment it was created this same session); a directory with a nested `AGENTS.md` was never scanned, so nothing in this project ever actually invoked it. Even fixed structurally, the mechanism was wrong: all three modes are live, turn-by-turn dialogue with the user in the foreground conversation — a Skill's job, not a scoped/semi-autonomous Agent's. The content itself needed no real rewrite (already shaped almost exactly like this project's skill convention — an entry file + reference files) — this was a conversion, not new authorship.
- Three modes, live coaching not decision-gating, never supplies the "right" answer: Design Walkthrough (explain an existing real-or-hypothetical design, gaps exposed not filled), Mock Interview (simulated system-design interview, in character, debrief only at the end), Tradeoff Defense (defend one architectural choice under "what if?" pressure).
- **No isolation screen / full `skill-interaction-testing` multi-agent run** — this is a straight structural conversion of pre-existing, well-formed content (the "notes on running this well" sections already showed real design thought), not new decision logic with a baseline-failure mode to demonstrate. Verification here is boundary-line reasoning by inspection instead, done carefully rather than skipped:
  - **`design-scoping`** — real overlap risk: "design a system for X" phrasing is common to both a real design ask and a rehearsal request. Fixed with a reciprocal clause in `design-scoping`'s own description naming the tell (real system about to be built vs. interview prep/practice on a hypothetical) and pointing to this skill.
  - **`explaining-my-work`** — the sharpest real collision found: that skill's own description already listed "preparing for an interview" as a trigger, which sits right on top of this skill's Mock Interview mode. Resolved as a hand-off, not a contradiction — `explaining-my-work` drafts the words (a script, talking points, something read or rehearsed solo); this skill actually runs the live back-and-forth rehearsal. Fixed with a reciprocal clause in `explaining-my-work` naming the distinction and the natural chain (draft there, then pressure-test here).
  - **`learning-gate`** — deliberately NOT given a Step 3 row, matching the precedent set by `skill-interaction-testing`/`catalog-drift-audit`: this skill's own three modes already embed the no-answer-giving, gap-exposing coaching discipline learning-gate would otherwise be deciding, so a second precondition would just re-ask an already-answered question.
  - `ticket-evaluation`, `technical-cost-decision`, `user-story-decomposition` — no concrete shared concept, correctly left untouched.
- Group: `Business/` (sibling to `explaining-my-work`, `software-carpentier-brand`).
- Source: not an `Architecture/` note — a `.claude/agents/system-design-communication/` directory the user copied in from an unnamed other project.
- Boundary lines: not real system-design work with real stakes (`design-scoping`); not a written deliverable about real completed work for a real audience (`explaining-my-work`, though the two can chain); not a second learning-intent gate (`learning-gate`, already superseded by this skill's own built-in discipline).
- **Recommend a real `skill-interaction-testing` pass the next time this skill's description changes** — today's verification was by-inspection only, not a live multi-agent run, same honest caveat applied to the `spec-drift-gate` execution-handoff extension earlier in this session.
- Memory: `skill-added-system-design-communication.md`.

### 18. `prompt-authoring` — rebuilt fresh from a deleted `_Other/` import

- [x] Built `2026-09-06` — `.claude/skills/Prompts/prompt-authoring/SKILL.md` (single-file). The authoring end of the prompt pipeline (`prompt-authoring` → `prompt-tester` → `prompt-archive`); `prompt-tester` and `prompt-archive` both already disclaimed "writing a new prompt from scratch", so the gap was acknowledged. Rebuilt from the deleted `_Other/` "Prompt optimizer" (`name: "48"`): kept its two hard rules (no placeholders ever; ship a finished prompt whatever the user gave — Case A bakes content in, Case B adds a self-gathering step), scope-every-instruction-explicitly, conditional reasoning nudge, chat-vs-API surface fit; dropped the Opus-4.8 lore and the mandatory closing line (model / pricing / thinking-effort config deferred to `claude-api`).
- Isolation screen **PASS** (1 worktree agent) — baseline reliably ships a `[paste X here]` template the user can't copy-send. `skill-interaction-testing` (same agent, 6 scenarios vs `prompt-tester` / `prompt-archive` / `ambiguity-gate` / cross-cutting gates / `claude-api`): CLEAN on 4, 2 preventive one-line carve-outs applied — `ambiguity-gate` Step 1 row routing "write/rewrite/improve a prompt" here; this skill's "Out of scope" → `learning-gate` for "teach me prompt principles".
- Group: `Prompts/`.
- Memory: `skill-added-prompt-authoring.md`.

### 19. `disclosure-gap-audit` — user-requested, from an "evaluate whether to build this skill" prompt

- [x] Built `2026-09-07` — `.claude/skills/Architecture/disclosure-gap-audit/` (SKILL + `disclosure-checklist.md` + `security-and-legal-passes.md` + README). Procedure skill (modelled on `change-surface-audit` / `failure-mode-analysis`), gated on a data inventory: reviews a shipped-or-shipping product against its public commitments and produces a ranked findings register. Three deliberately-unequal passes — **spine**: 15-probe policy-vs-practice walk (AI/LLM processing, model training, automated decisions, named subprocessors, retention, data-category completeness, cross-site tracking, children's data, international transfers, cookie categorization, biometric, drift, right-to-know mechanics, CCPA/CPRA sale-vs-share, effective-date/changelog); **secondary**: 8 security design-anti-patterns at design-review depth (secret in VCS, no authz check, IDOR, insecure defaults, missing rate limiting, PII in logs, encryption absent, over-broad scopes) with fix handoffs; **flag-only**: legal/compliance (age gate, accessibility, dark patterns, consent, IP/licensing) that names the regime and routes to counsel, never rules. Register columns: ID · Category (`policy-vs-practice-gap` / `security-anti-pattern` / `needs-legal-review`) · Finding · Evidence · Severity · Who-confirms · Handoff. User confirmed the scope cut (spine + secondary + flag-only, no WCAG audit / no pen-testing) and the `Architecture/` group before build.
- User's own decision-framing question ("does this leverage my skills / is it high-value for SWE-BSA-AI-eng / v1 scope / detection gaps / blunt yes-no") was answered first: **conditional YES**, scoped as above — leverages existing AppSec + fintech + BSA experience; the legal parts are contained to flag-only so they stay defensible.
- Isolation screen **MIXED** (3 worktree agents — baseline / clean-baseline / with-skill). The *clean* baseline (no leading self-assessment questions) is substantively strong on a well-informed prompt: it independently found the undisclosed-AI-subprocessor gap, the "we don't sell" vs Meta-pixel contradiction, cookie consent, DPIA, profiling/AI-Act, special-category data in receipts. What baseline lacked and the skill adds: the input gate that turns every unknown into a tracked `inventory-incomplete` finding, a 15-probe coverage guarantee (baseline hit ~8/14 organically), a trackable ranked register with per-row owner + handoff (baseline used prose High/Medium sections), and a disciplined flag-only line on legal conclusions (baseline made near-verdicts). Real improvement in rigor / coverage / trackability, not night-and-day on substance — recorded MIXED, not inflated. The with-skill run surfaced **6 defects, all fixed before interaction testing**: (1) "withholds the register" contradiction across the 3 files → reconciled to "gates on the inventory, caps dependent passes, never infers"; (2) "14 probes" but ~15 enumerated → promoted effective-date/changelog to a real probe 15; (3) probe 12 (drift) had no standalone-finding path → added cause-tag vs standalone modes; (4) security pass with no design doc → added "bundle into 4 themed findings, not 8 rows"; (5) "who confirms this" underspecified for `inventory-incomplete` → added the product/eng inventory-owner; (6) B2B/workforce-data angle (CA employees are "consumers" since 2023, controller/processor split) missing → added a checklist section + SKILL step-1 note.
- `skill-interaction-testing` (3 worktree agents, ~16 scenarios): **CLEAN** overall. Pool = `Architecture/` group (`change-surface-audit`, `design-scoping`, `failure-mode-analysis`, `access-control-modeling`, `cloud-iam-boundary`, `config-and-secrets-management`) + built-in `security-review` + the 4 cross-cutting gates. **1 real starvation fix**: adding a tracking pixel / analytics SDK trips `change-surface-audit` on the literal phrasing, but its description had no pointer to the disclosure half → added a reciprocal carve-out (description + Out-of-scope bullet + README handoff tree). **1 confirmed missing reciprocal**: `learning-gate` Step 3 row (+ line-58 parenthetical + line-86 Never list) — applied. **3 one-directional-pointer hardening edits** (repo convention — nobody pointed back at the new skill): `design-scoping` forward pointer (scope-here-audit-there), `access-control-modeling` "audit found a hole" trigger source, `failure-mode-analysis` "not an operational failure mode" carve-out. **2 gate-behaviour one-liners** added to the skill's own Step 2 from the multi-turn test: on refusal to supply the inventory, still produce a conditional probe-keyed register (never refuse outright, never fill holes with assumed values); a missing *verbatim policy* when one is stated to exist is `inventory-incomplete`, not a "no policy" High finding. Clean (no fix) vs `design-scoping` (greenfield → design-scoping), `security-review` (a diff → security-review), `failure-mode-analysis` (no disclosure signal → FMA keeps the "risk review" phrase), `access-control-modeling` (downstream hand-off works), `ambiguity-gate` ("review my app" bare → ambiguity-gate first), `problem-journal` / `problem-solving-gates` (no stacking).
- Group: `Architecture/`.
- Memory: `skill-added-disclosure-gap-audit.md`.

### 20. `history-integration-strategy` — user-requested, "would you make a skill around merge vs squash vs rebase"

- [x] Built `2026-09-07` — `.claude/skills/Git/history-integration-strategy/` (SKILL + README, no companion — matches `commit-and-push`'s single-file Git-group convention). Gate: withholds a merge/squash/rebase/fast-forward recommendation until five facts are stated — (1) is any commit being rewritten already shared, (2) does the repo/forge mandate a strategy, (3) is each commit meaningful or WIP noise, (4) integrating a finished branch vs. syncing an in-progress one, (5) does the team bisect / revert by feature. Then one recommendation + the deciding reason + the path-specific risk, not a survey. Ordered decision: mandated-strategy → shared-history-safety → sync-vs-integrate → a commit-quality × archaeology table for the integrate case, plus a "badly diverged long-lived branch → cut a fresh branch" escape. Found via `commit-and-push`'s own description already carving this out ("does NOT ... pick a branching strategy") with no skill to point at.
- Isolation screen **MIXED** (reasoned, not via separate worktree agents — the decision surface is small and well-bounded, and the token budget for this session was tight). On a vanilla prompt ("6 commits, some are WIP, merging to main — squash/rebase/merge?") a cold baseline is competent: the topic is well-trodden, it names the three options and reasonable trade-offs. What baseline reliably *misses*: the shared-branch safety check (the one destructive failure mode — rebasing/force-pushing commits others pulled), checking for a forge/repo mandate *first* (which moots the deliberation), and it defaults to a survey rather than one recommendation. Skill adds those three as an ordered gate + consistency across terse phrasings. Recorded MIXED — baseline isn't broken on the easy case, the skill's value concentrates on terse asks and shared/long-lived branches.
- `skill-interaction-testing` **CLEAN** (reasoned inline against the scoped pool: tier 1 `commit-and-push`; tier 2 `ambiguity-gate`, `learning-gate`, `problem-solving-gates`, `problem-journal`; tier 3 scan — no skill outside `Git/` touches branches/history, nothing pulled in). **3 reciprocal fixes**: (1) `commit-and-push` — description + both out-of-scope bullets ("History rewriting", "Branching / integration strategy") + README now name `history-integration-strategy` for the strategy choice (was one-directional — `commit-and-push` disclaimed it but pointed nowhere); (2) `learning-gate` Step 3 — added a row (merge-vs-rebase-as-concept → teach here; concrete integration → this gate) + the line-59 precondition-list clause; (3) `problem-solving-gates` Mode 2 (Options Generator) — added a clause routing "how a branch's history folds in" here, with a reciprocal carve-out in this skill's out-of-scope (the four integration options are a fixed set, not an open design space, so the "list your own options first" rep doesn't apply). Clean (no fix) vs `ambiguity-gate` (which-branch-into-which unclear → ambiguity-gate first, then this decides the method; ambiguity-gate names no successors by design) and `problem-journal` (no overlap — it captures errors / journals resolved problems).
- Group: `Git/`.
- Memory: `skill-added-history-integration-strategy.md`.

### 21. `codebase-file-orientation` — user-requested, "is there a skill for creating docs for new codebase files describing what a file consists of and does"

- [x] Built `2026-09-07` — `.claude/skills/Documents/codebase-file-orientation/` (SKILL + `orientation-doc-format.md` + README — matches `document-page-check`'s `Documents/` shape). Procedure, not a gate. Authors or reconciles a per-file companion **orientation doc**: a sidecar `.md` (matched to the repo's detected doc convention — sidecar / `docs/` mirror / central index / header banner — else a sidecar `<file>.md`) covering the file's role, entry points, dependencies in/out (incl. grepped inbound callers), and non-obvious gotchas. Framed throughout as **complementary to inline comments** (comments carry local "why", the doc carries file-level "where") — writing it is explicitly not licence to strip "why" comments. **Author** mode fills the fixed template and marks anything untraceable `TODO(author)` rather than inventing it. **Reconcile** mode diffs an existing doc's claims against the current file (role drift, stale/removed/new entry points + deps, fixed gotchas), emits a drift block, and proposes a patch without applying it. Scope decisions taken from the user via `AskUserQuestion`: sidecar-with-per-repo-detection output; trigger scoped to new/changed files only (pairs with the commit flow); Author + Reconcile both in scope.
- Isolation screen **MIXED** (reasoned inline, not separate worktree agents — small well-bounded surface, session token budget). A cold baseline on "write a doc for this file I just added" produces a competent prose description, so baseline isn't broken. What it reliably misses and the skill adds: inbound-caller tracing (not just what the file does), repo doc-convention detection instead of guessing where the doc goes, the "don't restate line-by-line / flag-don't-invent" discipline, the comment-vs-doc split, and a Reconcile mode for drift. Value concentrates on method, consistency, and drift-checking.
- `skill-interaction-testing` **CLEAN after fixes** (12 scenarios / 3 worktree agents). Pool: tier 1 `document-page-check`; tier 2 `ambiguity-gate`, `learning-gate`, `problem-solving-gates`, `problem-journal`; tier 3 `explaining-my-work`, `system-design-communication`, `spec-drift-gate`, `session-handoff`, `change-surface-audit`, `commit-and-push`. **1 real starvation fix**: "explain how this module works so I can check my understanding" pulled the new skill in and starved the `problem-solving-gates` Knowledge Checker rep → added a NOT-clause + Out-of-scope bullet carving out Knowledge Checker / `learning-gate`. **1 stacking risk fixed**: broadened the `session-handoff` carve-out so end-of-session wrap-up suppresses the skill entirely, not just the commit flow. **4 reciprocal-pointer fixes** (new skill pointed out, siblings didn't point back): `explaining-my-work` (faces a person vs. faces the codebase), `commit-and-push` (may offer per-file docs before the commit, never blocks), `change-surface-audit` (documents the file itself vs. traces blast radius — they compose), `document-page-check` (opposite direction, same group; README "no sibling overlaps" line updated). **`learning-gate`**: Step 3 row + line-60 precondition-list clause + "Never" list entry added. Clean vs `spec-drift-gate` (forward intent vs. backward description — the concrete-file-path input gate holds), `ambiguity-gate` ("document the stuff I changed" → one question; the skill's "not 'the stuff I changed'" clause keeps it from racing), `system-design-communication`, `problem-journal`.
- Group: `Documents/` (2nd skill; group subtitle broadened from "pre-flight checks on files before using them" to "producing and checking document files").
- Memory: `skill-added-codebase-file-orientation.md`.

### 22. `portfolio-thesis-audit` — user-requested, from an "Investment Strategy Master Context" doc's "Skills Still Needed" list (item 1, "URGENT, build first")

- [x] Built `2026-09-09` — `.claude/skills/Finance/portfolio-thesis-audit/SKILL.md` (single-file, matches `equity-trade-decision` / `seller-financing-evaluation`). Gate: for each position already held, withholds the keep/sell verdict until the user supplies three things themselves — a **falsifiable written thesis**, a **current-validity** call with evidence, and a **specific observable exit condition**. A position missing any of the three cannot be graded HOLD. Verdicts: **HOLD — thesis intact** / **EXIT — no surviving thesis** (no thesis, dead thesis, or "hold to get back to even") / **RESEARCH REQUIRED** (named task + a date, or it's a silent hold). Claude's contribution is narrow: judge falsifiability + observability, apply the rubric, and raise behavioral flags **as hypotheses** — disposition effect, luck-read-as-skill, informal-input-as-research, averaging-down-as-risk-management, return-target urgency distortion. Explicitly never authors the thesis. The mirror of `equity-trade-decision`: that skill sizes a position going in, this one re-examines positions already held.
- **Isolation screen: MIXED** (1 worktree baseline agent, non-consulting; skill behavior reasoned from the file). Baseline on a realistic 6-position prompt ("which to keep / sell", $12K/yr income target, positions with YouTuber / "I work in fintech" / "not sure why I hold it" rationales) was **strong on substance** — it did challenge the stated rationales, flagged the informal inputs, warned against reaching for yield given the between-jobs context. But it exhibited the exact failure the skill targets: by its own process notes it **wrote its own per-position thesis for all six**, handed down keep/trim/sell verdicts with no rep asked of the user, and left a vague non-verdict ("judgment call... keep only as an intentional growth holding") on the ambiguous one — a silent hold in disguise. The skill's gate + "never author the thesis" rule + time-boxed RESEARCH REQUIRED verdict directly fix that; its marginal value on *spotting* the behavioral patterns is smaller than expected (baseline already caught most). Recorded MIXED honestly, same family as `reliability-math` / `index-tuning` / `seller-financing-evaluation`.
- `skill-interaction-testing` **CLEAN after fixes** (10 scenarios, 1 worktree agent — skill committed to the branch first so the worktree wasn't stale). Pool: tier 1 `equity-trade-decision`, `seller-financing-evaluation`; tier 2 `ambiguity-gate`, `learning-gate`, `problem-solving-gates`, `problem-journal`; tier 3 scan — nothing pulled (no other skill shares "held position / ticker / P&L"). Clean with no fix: `equity-trade-decision` control (new entry with price/stop/capital stays there), `seller-financing-evaluation` control (business-purchase surface, no overlap), `learning-gate` conceptual control ("what makes a thesis vs a hunch, no positions in mind" → learning-gate, the carve-out is verbatim), `problem-solving-gates` (no mode matches a keep/sell decision), multi-turn gate release (turn 2 good-faith thesis → audit block, no re-gating). **6 reciprocal / hardening fixes applied:**
  1. `learning-gate` Step 3 — new row (review held positions → this gate) + `portfolio-thesis-audit`'s questions added to the line-63 don't-stack list. Required per `skill-architecture.md`; was entirely absent (the starvation pattern every prior new domain skill hit).
  2. `equity-trade-decision` — description back-pointer + a "Review a position already held" out-of-scope bullet. Was one-directional (this skill pointed to ETD three times; ETD never mentioned it). Also closes the S4 latent starvation: "average down on my loser" with price/stop/capital supplied would have let ETD's stronger trigger fire first and skip the disposition-effect flag → ETD bullet now routes a held-loser add through this audit first.
  3. `ambiguity-gate` Step 1 — hand-off row for "review/prune already-held positions"; reciprocal clause in this skill's description routing an unsettled "clean up my account" (thesis review vs. rebalance vs. consolidate vs. tax-loss harvest) to `ambiguity-gate` first. Fixes the S10 stacking/starvation coin-flip on "help me clean up my brokerage account".
  4. `problem-journal` — description clause + out-of-scope bullet carving out recurring *non-coding* mistakes (investing habits, behavioral patterns) → `learning-gate`. Its trigger phrases ("have I hit this before", "keeps happening", "want to learn from it") matched S8 almost verbatim despite its coding-error scope.
  5. This skill — explicit conversational-pressure clause ("just give me your call" / "I won't write a thesis" does not release the gate; blanket refusal → EXIT verdict, not a ticker read).
  6. This skill — "Coach the user out of a recurring pattern" out-of-scope bullet → `learning-gate`, not this skill and not `problem-journal`.
- Group: `Finance/` (3rd skill).
- Source: the user's pasted "Investment Strategy Master Context" doc (Sept 2026) — its `portfolio-thesis-audit` spec ("does a thesis exist / is it still valid / what is the exit condition" → hold / sell / research), its five named behavioral patterns, and its hard rules ("no position without a 3-sentence thesis + defined exit", "'averaging down' is not risk management", "return targets don't justify methodology shortcuts"). Built generic — the behavioral flags and the three-part precondition travel; no user-specific tickers baked in.
- Boundary lines: not a new entry / re-entry / sizing (`equity-trade-decision`); not options positions (assignment/theta not modelled — name the gap and stop); not portfolio-level allocation / diversification / rebalancing bands (grades each position on its own thesis, doesn't set the target shape); not tax-advantaged autopilot accounts (no per-position thesis); not a financial-advisor substitute; not a bare conceptual question (`learning-gate`).
- **Sibling future skills from the same doc:** `position-exit-rules` — **built 2026-09-09, item 23 below**. Still not built: `covered-call-decision` (the options-income skill this and ETD both disclaim), `watchlist-screener-criteria`, `weekly-portfolio-review`. Logged, not pointed at with live cross-refs until they exist.
- Memory: `skill-added-portfolio-thesis-audit.md`.

### 23. `position-exit-rules` — user-requested, "Investment Strategy Master Context" doc's "Skills Still Needed" list (item 2, "build in this order")

- [x] Built `2026-09-09` — `.claude/skills/Finance/position-exit-rules/SKILL.md` (single-file, matches the other three `Finance/` skills). Gate: withholds "the entry plan is complete" until the user commits, **in writing and as numbers**, to (1) a hard price stop — one number, no discretion, not a range or a "mental stop"; (2) thesis-invalidating events — the user states their own thesis clauses, then names one observable metric/event per clause that would disprove it, mapped to the clause (not generic "a bad quarter"); (3) a position-size ceiling — the % of the book that forces a trim regardless of conviction, which also caps how far a loser can be averaged down. Recommended 4th: an upside review trigger (a price/valuation that forces a thesis re-check, not an auto-sale) for symmetry against "luck read as skill". Claude checks each rule is a committed number or an observable clause-mapped event and produces the exit-rule block — it does **not** author the thesis or the events. This is the doc's "removes the disposition effect at the process level": the exit is defined pre-entry, while the user is calm.
- **Isolation screen: MIXED** (1 worktree baseline agent, non-consulting; skill behaviour reasoned from the file). Baseline on a realistic prompt (buy KLAR at $25, 3-pillar BNPL thesis, $45k account, "what should my exit rules be") produced a **very** strong framework — single committed stop ($17 weekly close), hard-exit vs. review-trigger separation, an 8%/10% size cap, invalidating events mapped to all three thesis pillars, a profit ladder + breakeven stop-raise. By its own process notes it **asked the user nothing and generated every invalidating condition itself**, and assumed risk tolerance / horizon / account type. That is precisely the failure the skill targets: a framework Claude wrote is not a pre-commitment the user owns and will honour under drawdown pressure. The skill's value is **process, not content quality** — forcing the user to author and commit each rule, and gating on the risk profile rather than assuming it. Recorded MIXED honestly, same family as `portfolio-thesis-audit` / `reliability-math` / `index-tuning`.
- `skill-interaction-testing` **CLEAN on all 10 scenarios, 5 reciprocal fixes** (1 worktree agent; skill committed to the branch first so the worktree wasn't stale). Pool: tier 1 `equity-trade-decision`, `portfolio-thesis-audit`, `seller-financing-evaluation`; tier 2 `ambiguity-gate`, `learning-gate`, `problem-solving-gates`, `problem-journal`; tier 3 — nothing pulled. Controls clean (solo new-skill; solo ETD with a stop already given → new skill correctly silent). Gate holds under pressure ("just give me a stop %" → asks for one committed number + ≥1 clause-mapped event, doesn't cave to "15–20%"). Gate releases cleanly on a good-faith turn-2 answer, no skill re-firing. Clean vs `learning-gate` (conceptual "how do trailing stops work" stays there — carve-out present 3×), `ambiguity-gate` ("tighten up my risk management" → ambiguity-gate owns it, new skill doesn't over-claim), `seller-financing-evaluation` (no surface). Every finding was the same shape: **the new skill's declared chains were outbound-only, referenced by zero siblings.** Fixes:
  1. `equity-trade-decision` — description clause + "What this does not do" bullet + a pointer on the "If no stop-loss is set, say so and stop" line. The forward chain (position-exit-rules' price stop → ETD's stop-loss sizing input) was wired only from this skill's side; ETD dead-ended by prompting for a bare stop number — exactly the ad-hoc stop the new skill exists to prevent.
  2. `portfolio-thesis-audit` — RESEARCH REQUIRED rubric row now names `position-exit-rules` as the specific task when the gap is a missing exit condition; new "Build a missing exit rule" out-of-scope bullet; description clause; Portability line. PTA's rubric previously said "name the specific task and a date" with no skill named — a dead end.
  3. `learning-gate` — Step 3 row (define a position's exit rules → this gate) + the line-64 don't-stack enumeration. Required by `skill-architecture.md`; was absent (the starvation pattern every new domain skill hits).
  4. This skill — defensive carve-out vs `problem-solving-gates` ("why do I keep getting whipsawed" post-mortem is not software debugging; the job is the rule set).
  5. This skill — rule 1 now also asks the user to pre-commit whether/at what gain the stop moves up (breakeven stop-raise), from the baseline's stronger treatment of that.
- Pairs recorded tested-and-clean (`position-exit-rules` ×): `equity-trade-decision`, `portfolio-thesis-audit`, `seller-financing-evaluation`, `learning-gate`, `problem-solving-gates`, `ambiguity-gate`, `problem-journal`.
- Group: `Finance/` (4th skill).
- Source: the user's "Investment Strategy Master Context" doc (Sept 2026) — its `position-exit-rules` spec ("price drops X% below entry, thesis-invalidating event, position exceeds Y% of portfolio", "eliminates the disposition effect at the process level") and hard rules ("no new position without ... a defined exit condition", "'averaging down' is not risk management"). Built generic — no user tickers baked in.
- Boundary lines: not sizing / the pre-trade checklist (`equity-trade-decision`, which consumes the price stop); not judging a held position's thesis (`portfolio-thesis-audit`, which routes here for a missing exit rule); not options exit logic (`covered-call-decision`, not built — name the gap); not tax-lot / sell mechanics; not portfolio target allocation (the size ceiling is a per-position guardrail); not a bare conceptual question (`learning-gate`); not a financial-advisor substitute.
- **Sibling future skills from the same doc:** `covered-call-decision` — **built 2026-09-09, item 24 below**. Still not built: `watchlist-screener-criteria`, `weekly-portfolio-review`.
- Memory: `skill-added-position-exit-rules.md`.

### 24. `covered-call-decision` — user-requested, "Investment Strategy Master Context" doc's "Skills Still Needed" list (item 3, "build in this order")

- [x] Built `2026-09-09` — `.claude/skills/Finance/covered-call-decision/SKILL.md` (single-file, matches the other four `Finance/` skills). The options skill `equity-trade-decision` and `portfolio-thesis-audit` both explicitly disclaim. Gate: before naming a strike, runs a **six-check qualification screen** — (1) ≥100 shares; (2) a liquid options chain (tight bid/ask, real open interest); (3) the user genuinely willing to be assigned at a realistic strike *and* this not being the highest-conviction "let it run" name; (4) a strike that can sit at/above the user's own price target (from `position-exit-rules` / `portfolio-thesis-audit`) so the call isn't written against the thesis; (5) not the top-conviction position; (6) assignment tax character understood (LT/ST, taxable vs tax-advantaged). A fail on "willing to be assigned" / "strike ≥ target" / "not top-conviction" is a **stop — recommend a different position, not a different strike.** Only for a qualifying position: strike (vs. target + assignment probability), expiration (DTE, earnings, ex-dividend early-assignment, qualified-covered-call holding-period note), contract count (all vs. a portion), premium math (received, % of underlying, annualized *with* the explicit "not guaranteed, not downside protection" caveat, breakeven, static vs if-called return), and the assignment outcome (cash, realized gain + tax character, thesis position closed, wash-sale on re-entry).
- **Isolation screen: MIXED** (1 worktree baseline agent, non-consulting; skill behaviour reasoned from the file). Baseline on a realistic prompt (200 AXP shares, $150 basis, ~$240 now, LT, taxable, "$12k/yr income target on a $45k account, what call do I write") was **substantively excellent** — it led with the $12k target being a ~25% distribution rate that covered calls can't safely produce, addressed the ~$20k realized LT gain on assignment and its bracket-dependence, flagged the concentration, and explicitly did *not* treat annualized premium as reliable income. But by its own process notes it **asked nothing and assumed every input** (price, IV, dividend, income level, options approval), and — critically — **gave the strikes first and treated "should you write on this position at all" as after-the-fact caveats** rather than a blocking screen ("I did not require the user to state a conviction level or a sell target before providing strike numbers"). The skill's value is the **gate ordering**: screen → stop-or-proceed → block, and a "this position doesn't qualify, write on a different one" outcome the baseline never produces. Recorded MIXED honestly, same family as `portfolio-thesis-audit` / `position-exit-rules` / `reliability-math`.
- `skill-interaction-testing` **CLEAN on all 11 scenarios, 5 fixes** (1 worktree agent; skill committed to the branch first, no worktree-staleness). Pool: tier 1 `equity-trade-decision`, `portfolio-thesis-audit`, `position-exit-rules`, `seller-financing-evaluation`; tier 2 `ambiguity-gate`, `learning-gate`, `problem-solving-gates`, `problem-journal`; tier 3 — nothing pulled. Controls clean (solo new skill; solo ETD on a plain buy). Gate holds under "just tell me the strike and premium, skip the screening" pressure. Gate releases cleanly on a good-faith multi-turn answer. Clean vs `learning-gate` ("how do covered calls work, don't own anything" stays there — carve-out present 3×), `ambiguity-gate` ("set up an income strategy for my portfolio" → ambiguity-gate owns it, new skill's triggers are all covered-call-specific), `seller-financing-evaluation` (no shared vocabulary). Every finding was the same shape — **the new skill's three declared chains were outbound-only; all three siblings' options carve-outs still said only "name the gap and stop"** (`position-exit-rules`' *body* named it, its description didn't). Fixes:
  1. `equity-trade-decision` — options carve-out (description + body bullet) now names `covered-call-decision` as the hand-off for a covered-call plan on shares it would buy.
  2. `portfolio-thesis-audit` — options carve-out (description + body bullet) now names `covered-call-decision` + "run the audit first, then hand off".
  3. `position-exit-rules` — description options clause now names `covered-call-decision` (body already did).
  4. `learning-gate` — Step 3 row + line-65 don't-stack enumeration. Required by `skill-architecture.md`; was absent (the starvation pattern every new domain skill hits).
  5. This skill — three self-hardening edits: an explicit "just tell me the strike" pressure line (mirrors `portfolio-thesis-audit`); a diagnostic-framing trigger ("my calls keep getting assigned before a pullback, what am I doing wrong" → re-run the screen against the next write); carve-outs vs `problem-solving-gates` (software-only) and `ambiguity-gate` (portfolio-wide income-strategy disambiguation). Also folded conviction level into precondition input 3 (was only screen line 5, so a good-faith reply now clears in one round) and added the qualified-covered-call holding-period line to the block, from the baseline's stronger tax treatment.
- Pairs recorded tested-and-clean (`covered-call-decision` ×): `equity-trade-decision`, `portfolio-thesis-audit`, `position-exit-rules`, `seller-financing-evaluation`, `learning-gate`, `problem-solving-gates`, `ambiguity-gate`, `problem-journal`.
- Group: `Finance/` (5th skill).
- Source: the user's "Investment Strategy Master Context" doc (Sept 2026) — its `covered-call-decision` spec ("which positions qualify, strike selection relative to price target, expiration selection, premium income calculation, assignment risk, what 'called away' means for the portfolio") and Bucket 2 ("covered calls on stable existing positions ... 1–2% monthly premium ... the realistic path to the $12K target without requiring 21.8% price appreciation"). Built generic — no user tickers baked in.
- Boundary lines: not the underlying stock entry/sizing (`equity-trade-decision`, which excludes options and hands off here); not the keep-the-shares decision (`portfolio-thesis-audit`, upstream); not the equity exit rules (`position-exit-rules`, whose price target is the strike floor); not any non-covered-call options structure (CSPs, spreads, the wheel, naked, multi-leg, long options, LEAPS — name the gap and stop); not an options tutorial (`learning-gate`); not a covered-call *strategy* post-mortem treated as software debugging (`problem-solving-gates` is software-only — this owns it); not tax/legal advice (flags LT/ST + wash-sale, names a CPA).
- **Sibling future skills from the same doc:** `watchlist-screener-criteria` — **built 2026-09-09, item 25 below**. Still not built: `weekly-portfolio-review` (Python-automatable checklist).
- Memory: `skill-added-covered-call-decision.md`.

### 25. `watchlist-screener-criteria` — user-requested, "Investment Strategy Master Context" doc's "Skills Still Needed" list (item 4, "build in this order")

- [x] Built `2026-09-09` — `.claude/skills/Finance/watchlist-screener-criteria/SKILL.md` (single-file, matches the group). "The gate before `equity-trade-decision`'s gate" — a numeric entry filter that replaces YouTube / product familiarity as the reason a name enters consideration. **Two modes.** *Define* — the user commits a stated style (value / quality compounder / growth / momentum / dividend) + roughly 4–8 criteria, each `metric + numeric threshold + direction + one line on why it predicts returns`, + a hard-disqualifier list + a review cadence. Claude checks each criterion is numeric and checkable, that the set coheres around the style, and produces the written screen block — it does **not** supply the thresholds. *Screen* — a specific ticker is run against an existing screen with **user-supplied** metric values (Claude never fetches or estimates fundamentals): pass/fail per line, a verdict (add to watchlist / proceed to `equity-trade-decision` / reject), and an explicit **informal-input flag** when a narrative-sourced candidate is failing the filters. No screen yet + a candidate → gate back to Define. Includes an intake-log / rejected-list mechanic so it's a process, not a one-off vet.
- **Isolation screen: MIXED, leaning weak — the strongest baseline of the Finance batch** (1 worktree agent, non-consulting). On "help me set up screening criteria so I stop picking stocks off YouTube", the unaided baseline did almost everything the skill is built to force: opened with the two framing questions (what drives returns → style; constraints / circle of competence), gave threshold *ranges* only as labelled reference points and explicitly made the user commit their own numbers + a rationale each, built in a single intake funnel every name passes through regardless of source with "I like the product" demoted to one qualitative input, a rejected list, hard disqualifiers, and a quarterly-screen / annual-criteria-review cadence, and framed the whole thing as user-owned and versioned. Its **only** self-identified gap: "I did not stop and wait; in the same reply I also handed over a full style-branched scaffold so you're not blocked." So the skill's marginal value is narrower than the earlier three: it actually gates (style answer + per-criterion commitment before the block, no scaffold-to-unblock), it gives *Screen* mode a reusable per-candidate output artifact with the informal-input flag (the baseline described the mechanism but produced no format), and consistency (a differently-primed baseline could hand over a canned screen). Recorded honestly — the baseline is not broken; same "value concentrates on method + consistency" note as `reliability-math` / `history-integration-strategy` / `codebase-file-orientation`.
- `skill-interaction-testing` **CLEAN on all 12 scenarios, 7 fixes** (1 worktree agent; skill committed to the branch first). Pool: tier 1 `equity-trade-decision`, `portfolio-thesis-audit`, `position-exit-rules`, `covered-call-decision`, `seller-financing-evaluation`; tier 2 `ambiguity-gate`, `learning-gate`, `problem-solving-gates`, `problem-journal`; tier 3 — nothing pulled. Controls clean (Define-only; Screen-only with an inline screen; narrative-sourced candidate → the skill's core case; `equity-trade-decision` on a real trade). Gate holds under "just give me a good screen, I don't want to design my own". Gate releases cleanly on a good-faith Define answer. Clean vs `learning-gate` (conceptual "what ROIC threshold do people use" stays there — carve-out verbatim), `ambiguity-gate` ("be more disciplined about my investing" → ambiguity-gate owns it, the new skill's triggers are all screen-specific), `seller-financing-evaluation`. Fixes:
  1. `equity-trade-decision` — description clause + a line in pre-trade-checklist item 1 + a red flag: a narrative-sourced name that never cleared a numeric screen runs `watchlist-screener-criteria` first. The "gate before the gate" pointer was entirely one-directional (only in the new skill).
  2. `learning-gate` — Step 3 row + line-66 don't-stack enumeration. Required by `skill-architecture.md`; absent. This was also the mechanism by which scenario 8 starved.
  3. `problem-journal` — description + the non-coding-mistake out-of-scope bullet now name `watchlist-screener-criteria` for the specific "I keep buying names off tips or videos" pattern (both previously routed it to `learning-gate` only — written before this skill existed).
  4. `portfolio-thesis-audit` — the "coach the user out of a recurring pattern" bullet now names `watchlist-screener-criteria` for a standing "I keep buying YouTube stocks" process gap (same stale-carve-out fix).
  5. This skill — description carve-outs vs `problem-solving-gates` Options Generator / `tech-decision-walkthrough` (the "what are my options for X, which should I pick" phrasing collides with the `tech-decision-walkthrough` row; choosing a screening *style* is Define mode) and vs private-business-acquisition screening (`seller-financing-evaluation` — public-equity tickers only).
  6. This skill — explicit "just give me a good screen / I don't want to design my own" pressure clause (description + Red flags), mirroring `portfolio-thesis-audit` / `covered-call-decision`.
  7. This skill — a screen stated inline counts as an existing screen (run Screen mode against it, don't gate back to a full Define pass); plus the intake-log / rejected-list mechanic folded in from the baseline.
- Pairs recorded tested-and-clean (`watchlist-screener-criteria` ×): `equity-trade-decision`, `portfolio-thesis-audit`, `position-exit-rules`, `covered-call-decision`, `seller-financing-evaluation`, `learning-gate`, `problem-solving-gates`, `ambiguity-gate`, `problem-journal`.
- Group: `Finance/` (6th skill).
- Source: the user's "Investment Strategy Master Context" doc (Sept 2026) — its `watchlist-screener-criteria` spec ("quantitative entry filters before a stock reaches trade-decision stage — the gate before the gate — replaces YouTube and familiarity as the primary filter") and behavioral pattern 4 ("informal inputs treated as research ... produces neither repeatable edge nor testable criteria"). Built generic — no user tickers or thresholds baked in.
- Boundary lines: not the specific-trade evaluation/sizing that comes after (`equity-trade-decision`); not held positions (`portfolio-thesis-audit`) or exits (`position-exit-rules`); not a stock-picking service (the user screens in their own tool and brings candidates; Claude never fetches/estimates fundamentals); not backtesting the screen; not portfolio construction; not a screening-*style* options question dressed as an architecture choice (`problem-solving-gates` / `tech-decision-walkthrough` are software-only); not private-business M&A screening (`seller-financing-evaluation`); not a bare concept question (`learning-gate`).
- **Sibling future skill from the same doc:** `weekly-portfolio-review` — **built 2026-09-09, item 26 below. That completes all five skills on the doc's "Skills Still Needed" list.**
- Memory: `skill-added-watchlist-screener-criteria.md`.

### 26. `weekly-portfolio-review` — user-requested, "Investment Strategy Master Context" doc's "Skills Still Needed" list (item 5, last on the list)

- [x] Built `2026-09-09` — `.claude/skills/Finance/weekly-portfolio-review/SKILL.md` (single-file). **Procedure, not a gate** (like `index-tuning` / `failure-mode-analysis` / `disclosure-gap-audit`) — the recurring integration pass over the other five `Finance/` skills. Needs concrete inputs (holdings table with cost basis / price / % of book; per-position thesis + exit-rules status on file, or a flag they're missing; open covered calls with strike + expiration; a forward calendar of earnings / ex-div / expiration dates; the last-review date) then runs a fixed **seven-step walk**: (1) positions vs. thesis → `portfolio-thesis-audit`; (2) stops still valid → `position-exit-rules`; (3) options expiring, ITM/OTM + ex-div early-assignment → `covered-call-decision`; (4) earnings/events ahead; (5) allocation drift vs. each position's size ceiling → trim flag; (6) watchlist names in range → `equity-trade-decision`; (7) since-last-review delta. **It surfaces and routes — it does not decide inline**; every action item in the report is tagged with the owning skill. Runs on a calendar cadence on purpose (review when calm, not only when a position drops). Carries an **automation-boundary table** (script does the data — snapshot, weights, price-vs-stop arithmetic, calendar pull, ITM/OTM status; human does the judgment — thesis intact?, roll vs. assign, trim now or wait, sizing) mirroring the doc's own Automate / Don't-automate split, and a `<n>/<n> theses+stops on file` line that re-surfaces the coverage gap every week.
- **Isolation screen: MIXED, leaning weak — the second consecutive very strong baseline** (1 worktree agent, non-consulting). On "what should a weekly portfolio review cover, step by step", the unaided baseline produced essentially the whole thing: schedule-not-drop framing up front and repeated, a mechanical snapshot step, a since-last-review delta step, per-position thesis check that *routes* ("route, don't trade") rather than trading inline, covered calls split ITM/OTM with roll-vs-assign + tax + ex-div early assignment, a rolling 4–8 week events calendar, an action list with triggers, a decision log, and its own script-vs-human-judgment section. It did **not** ask for the actual holdings first (produced a template — defensible given the ask was for the checklist structure). So the skill's marginal value is narrow and specific: it runs against the *real* book rather than a template; it produces a **consistent report format with each action item tagged to the owning skill by name** (the baseline routes "to a separate deliberate decision" but names no skill and no repeatable format); the persistent `<n>/<n> on file` nag; and — the actual justification — it exists as the **named recurring integration point for the five-skill Finance suite**, a role an unaided baseline can't fill because it doesn't know those five skills exist. Recorded honestly, same "baseline not broken, value is method + consistency + integration" note as `watchlist-screener-criteria` / `reliability-math` / `codebase-file-orientation`.
- `skill-interaction-testing` **CLEAN on all 13 scenarios, 8 fixes** (1 worktree agent; skill committed to the branch first). Pool: tier 1 all five other `Finance/` skills + `seller-financing-evaluation`; tier 2 `ambiguity-gate`, `learning-gate`, `problem-solving-gates`, `problem-journal`; tier 3 `session-handoff` (pulled — "wrap up / pick it up next week" shares the persist-a-review concept). **No stacking anywhere** — the routing is flag-and-tag, not gate-invocation, so the procedure never runs a downstream skill's precondition questions (confirmed on the thesis, options, and missing-stop hand-offs). All findings were discoverability / reciprocity:
  1. **`portfolio-thesis-audit` owns the literal phrase "review my portfolio"** and had zero awareness of this skill. Fixed both directions: this skill's description now says a bare "review my portfolio" with no cadence signal is `portfolio-thesis-audit` / `ambiguity-gate`; `portfolio-thesis-audit` gained a description clause + a "does not do" bullet routing the scheduled whole-account pass here.
  2. `learning-gate` — Step 3 row + line-67 don't-stack enumeration (procedure variant — cede the coaching level). Every other Finance skill had a row; this didn't.
  3. This skill — diagnostic-framing trigger added ("I keep skipping my review", "things fall through the cracks", "my weekly process isn't working"), mirroring `covered-call-decision`'s pattern, so scenario 9 doesn't dead-end at a generic answer.
  4. `covered-call-decision` — a note that when the position is an already-open call being managed into expiration (roll vs. assign, e.g. routed from step 3), the share-lot / liquidity / conviction screen lines are a confirmation, not a re-derivation.
  5. Reciprocal one-liners from the routed-to gates: `equity-trade-decision` ("a name surfaced by `weekly-portfolio-review`'s watchlist step arrives already screened"), `watchlist-screener-criteria` ("re-checks already-cleared names for entry-range moves"), `position-exit-rules` ("step 2 routes here for a held position with no stop or a stale one").
  6. `session-handoff` cross-note in this skill's Portability (run/confirm the review, then persist via `session-handoff`).
- Pairs recorded tested-and-clean (`weekly-portfolio-review` ×): `portfolio-thesis-audit`, `position-exit-rules`, `covered-call-decision`, `equity-trade-decision`, `watchlist-screener-criteria`, `seller-financing-evaluation`, `learning-gate`, `problem-solving-gates`, `ambiguity-gate`, `problem-journal`, `session-handoff`.
- Group: `Finance/` (7th skill; group subtitle broadened from "personal/business-finance decisions" to "... decisions and reviews").
- Source: the user's "Investment Strategy Master Context" doc (Sept 2026) — its `weekly-portfolio-review` spec ("structured weekly checklist: positions vs thesis, options expiring, earnings upcoming, stop-losses still valid — designed to be automated with Python once defined") and the Tooling Roadmap's Automate / Do-Not-Automate-Yet table. Built generic.
- Boundary lines: not the per-position keep/sell audit (`portfolio-thesis-audit` — this flags positions *for* it); not portfolio construction / target allocation; not the scheduling/automation infra itself (defines the review's content, a script feeds it); not a bare conceptual question (`learning-gate`); not session wrap-up (`session-handoff` — though a review report can be persisted through it).
- **This completes the doc's "Skills Still Needed" list** — `portfolio-thesis-audit` (22), `position-exit-rules` (23), `covered-call-decision` (24), `watchlist-screener-criteria` (25), `weekly-portfolio-review` (26). The five compose into one equity-investing lifecycle: screen a candidate → size and enter it → define its exits → (write calls on it) → review the book weekly → audit any position whose thesis wobbles.
- Memory: `skill-added-weekly-portfolio-review.md`.

### 27. `equity-research-writeup` — Claude-recommended (post-doc-list), user picked it first from a shortlist of three (`equity-research-writeup` / `asset-allocation-policy` / `etf-selection`)

- [x] Built `2026-09-09` — `.claude/skills/Finance/equity-research-writeup/SKILL.md` (single-file). Recommended after the user asked, having merged all five doc skills, what other Finance skills and improvements were worth making — naming "no research process in place" and "not much fundamental work" as the gap. This fills the hole *between* `watchlist-screener-criteria` (numeric pre-filter) and `equity-trade-decision` (whose pre-trade checklist item 1 was a bare "reviewed the 10-K, key takeaway" checkbox) and `position-exit-rules` (whose rule 2 assumed "the user first states the clauses of their own thesis" — with nothing producing that thesis).
- **Gate.** Withholds a finished single-company research writeup — and refuses to author the analysis — until the user fills **eight sections** in their own words, each figure tied to a named source: (1) the business; (2) unit economics — the 2–3 P&L drivers with values; (3) competitive position — the *evidence* of pricing power / retention / share, not the word "moat"; (4) 3–5y financial trend from the filings; (5) valuation reasoning (multiple vs history + peers, implied expectations, yield) — *not* a DCF spreadsheet; (6) the user's own **strongest** bear case; (7) a falsifiable ≥3-sentence thesis; (8) what-would-change-my-mind, clause-mapped to (7). Claude supplies the skeleton, checks every claim has a figure + named source, flags THIN / UNSOURCED / ALL-BULL sections, pressure-tests the bear case, and offers its own knowledge only labelled *unverified — check the filing*. It does not write a section, supply a figure, run a valuation, or fetch data. Sections 7–8 feed forward: 7 → `equity-trade-decision` checklist item 1; 8 → `position-exit-rules` rule 2 (thesis-invalidating events), pasted not re-derived.
- **Isolation screen: PASS** (first clean pass of this Finance batch; 1 worktree agent, non-consulting, with web access). Asked to "walk me through a real fundamental analysis" of NVDA, the baseline **wrote the entire ~2,000-word analysis itself**, produced a scenario valuation *and* a reverse-DCF, and gave a personal price view — then bolted "now write your own one-sentence thesis" on as a closing coda. Its own process notes: *"I produced the full fundamental analysis myself… I did not make the user do the reading or writing."* That is the exact inversion the gate fixes. The baseline had good instincts (it flagged which figures were memory-vs-sourced and told the user to verify against EDGAR; it asked for the user's own bear case at the end) — noted so the PASS isn't oversold — but it front-loaded the authoring, which is the failure. The skill withholds the analysis and makes the eight sections the user's work first.
- `skill-interaction-testing` **CLEAN on all 13 scenarios, 10 edits** (1 worktree agent; skill committed first). Pool: tier 1 all seven other `Finance/` skills; tier 2 `ambiguity-gate`, `learning-gate`, `problem-solving-gates`; tier 3 nothing. **No contradictions, no true starvation** — the new skill's own carve-outs (ETF, private company, conceptual question, held-position, screener, DCF, sizing) were all vindicated in the control scenarios. Every finding was a **missing reverse pointer** (all five of the new skill's feed-forward/hand-off claims were one-directional) or a **tone issue in the new skill's own red-flags list**. Edits:
  1. `equity-trade-decision` — checklist item 1 + description now name `equity-research-writeup` as the structured form of the fundamentals review; a bare "I read it, looks solid" no longer satisfies the item.
  2. `position-exit-rules` — rule 2 + description + Portability: if `equity-research-writeup` was done, its section 8 *is* the rule-2 list (paste, don't re-derive).
  3. `learning-gate` — Step 3 row + don't-stack enumeration.
  4. `watchlist-screener-criteria` — description + Screen-mode verdict + Portability: the pipeline is now screen → `equity-research-writeup` → `equity-trade-decision` (was screen → trade).
  5. `portfolio-thesis-audit` — new "Author the thesis from scratch" out-of-scope bullet + a RESEARCH REQUIRED rubric clause (unwritten thesis / unread financials → run `equity-research-writeup` by a date) + a description trigger clause disambiguating "research my X position" when X is *held* (starts at the audit, which may route back here).
  6. `weekly-portfolio-review` — input 2 now credits `equity-research-writeup` for building the thesis.
  7. This skill — softened the screen-first note (moved the "unscreened name" bullet out of the "writeup isn't real" invalidator list to a soft flag; reworded the Output template line) so a full writeup on an unscreened name isn't treated as illegitimate.
  8. This skill — added the sibling-standard "pressure is not a release of the gate" clause and extended the `learning-gate` carve-out examples ("how does a DCF work", "help me get better at analysing companies").
- Pairs recorded tested-and-clean (`equity-research-writeup` ×): `watchlist-screener-criteria`, `equity-trade-decision`, `position-exit-rules`, `portfolio-thesis-audit`, `covered-call-decision`, `weekly-portfolio-review`, `seller-financing-evaluation`, `learning-gate`, `problem-solving-gates`, `ambiguity-gate`.
- Group: `Finance/` (8th skill). The equity pipeline is now: `watchlist-screener-criteria` → **`equity-research-writeup`** → `position-exit-rules` → `equity-trade-decision` → (`covered-call-decision`) → `weekly-portfolio-review` → `portfolio-thesis-audit`.
- **Still recommended, not built** (2 of 3 done): `asset-allocation-policy` — **built 2026-09-09, item 28 below**. Still open: `etf-selection` (evaluate a fund on expense ratio / index methodology / AUM / tracking error / holdings overlap — the user holds ETFs and nothing covers them; `equity-research-writeup`, `portfolio-thesis-audit`, and `asset-allocation-policy` all explicitly punt fund-ticker choice). A lighter `tax-aware-selling` (lot selection, wash-sale window, LT/ST, low-income-year LTCG harvesting) floated as lower priority / possible `weekly-portfolio-review` mode.
- Memory: `skill-added-equity-research-writeup.md`.

### 28. `asset-allocation-policy` — Claude-recommended (2nd of the three-skill shortlist), user picked it next

- [x] Built `2026-09-09` — `.claude/skills/Finance/asset-allocation-policy/SKILL.md` (single-file). The whole-book target shape that **every** position-level `Finance/` skill explicitly defers: `equity-trade-decision`, `portfolio-thesis-audit`, `watchlist-screener-criteria`, `position-exit-rules`, and `weekly-portfolio-review` all carried a "not portfolio-level allocation / diversification-vs-concentration policy / rebalancing bands" carve-out that, before this, **pointed nowhere**. Also the home for the `Diversification.md` concentration-vs-diversification tension logged unresolved in item 11 — the skill forces the user to *choose* rather than resolving it.
- **Gate.** Withholds an allocation recommendation — and refuses target %s off a risk-tolerance quiz — until the user supplies five inputs: (1) the capital map (runway / near-term-need / investable, the last broken out by account: taxable, Roth/IRA, 401k); (2) the withdrawal plan + horizon, stated as a % of the pool and reconciled out loud as an income plan vs. a drawdown plan; (3) risk **capacity** (can the withdrawal plan survive a 40% equity drawdown) vs. tolerance — capacity binds; (4) an explicit concentrated-vs-diversified stance (the skill will not pick); (5) bucket purposes if they run buckets. Then the user commits a six-area policy — asset-class targets + bands, within-equities split (index / individual / active sleeve), position-sizing bands, diversification limits (per-sector / per-theme / geography / a named correlation check), the rebalancing trigger + tax-aware ordering, and contribution routing — and Claude checks it is complete, internally consistent (equity sub-splits sum to the equity target; the cash sleeve actually covers the withdrawal + buffer; the single-name cap matches the concentration stance), and stress-tests it against the withdrawal plan and capacity. Claude supplies no target numbers.
- **Isolation screen: MIXED, leaning weak — the fourth consecutive strong "help me set X" baseline** (1 worktree agent, non-consulting). On "$45k brokerage / never set a target allocation / between jobs / $30k emergency fund", the unaided baseline: asked for horizon + burn-rate + search-length first; put concentrated-vs-diversified as an explicit "your call"; **reclassified the brokerage T-bills as runway and recomputed the true investable pool at ~$30–35k, not $45k**; produced position-sizing bands, semi-annual + breach-triggered rebalancing with new-money-first + tax-aware ordering, and a ranked contribution routing order; flagged the low-income year for 0%-bracket gain realisation. Its gap (own notes): it **still handed over concrete target %s itself** ("70% equity core, 25% bonds, 5% metals … one option contingent on assumptions") and supplied a recommended concentration default rather than leaving the choice fully the user's. So — same pattern as `watchlist-screener-criteria` / `weekly-portfolio-review` / `equity-research-writeup`'s isolation runs: a competent unaided model gives a genuinely good answer to "help me set X"; the skill's value concentrates on **gating the numbers to the user** (a "default you can run with" becomes the allocation by default, not by decision), the **reusable policy artifact** with the consistency + stress-test checks, being the **named frame** `position-exit-rules` rule 3 and `weekly-portfolio-review` step 5 reference, and **consistency** (a differently-primed baseline could hand "60/40 for your age" with no capital map). Recorded honestly.
- `skill-interaction-testing` **CLEAN on all 13 scenarios, ~11 edits** (1 worktree agent; skill committed first). Pool: tier 1 all eight other `Finance/` skills; tier 2 `ambiguity-gate`, `learning-gate`, `problem-solving-gates`. **No contradictions, no hard starvation.** The gate holds under "just tell me a good split for someone my age"; releases cleanly on a good-faith five-input answer and the stress test fires on a $12k/27%-of-pool withdrawal + "a 40% drop would force me to sell". The pattern was uniform: **six missing inbound pointers, two of which back claims the new skill already makes** ("the bands are what `weekly-portfolio-review` step 5 checks drift against" was not yet true; "the house default `position-exit-rules` rule 3 references" was overstated). Fixes:
  1. `weekly-portfolio-review` — step 5 now checks drift vs. the `asset-allocation-policy` bands (asset-class / sector / theme / geography) and routes to it to *build* a policy when none exists; description disclaimer, the step-5 body, an `[asset-allocation-policy]` action-item tag, and an automation-table row all updated (this integration was asserted by the new skill but did not exist).
  2. `position-exit-rules` — rule 3 / the "Portfolio target allocation" bullet / the "standing house rules" paragraph now name `asset-allocation-policy` area 3 as where the portfolio-wide single-name cap is set.
  3. `equity-trade-decision`, `portfolio-thesis-audit`, `watchlist-screener-criteria` — the three "not portfolio-level allocation policy" carve-outs (description + body bullet each) now name `asset-allocation-policy`; `portfolio-thesis-audit` also gets a one-line "audit positions first, then allocation on survivors" sequence note.
  4. `learning-gate` — Step 3 row + don't-stack enumeration.
  5. This skill — extended the skip-pressure line ("a rule of thumb for someone my age", "60/40 is fine, right?"); a "don't run the full five-input gate for one band requested mid-`position-exit-rules`" note; carve-outs for *executing* a rebalance (vs. setting the policy), fund-ticker choice (SPY-vs-VOO — separate, and `etf-selection` isn't built), allocating a business purchase price (`seller-financing-evaluation`), and an unsettled vague ask (`ambiguity-gate` first).
- Pairs recorded tested-and-clean (`asset-allocation-policy` ×): `equity-trade-decision`, `position-exit-rules`, `portfolio-thesis-audit`, `watchlist-screener-criteria`, `equity-research-writeup`, `covered-call-decision`, `weekly-portfolio-review`, `seller-financing-evaluation`, `learning-gate`, `problem-solving-gates`, `ambiguity-gate`.
- Group: `Finance/` (9th skill). It is the frame the equity pipeline operates inside: `asset-allocation-policy` sets the bands; `watchlist-screener-criteria` → `equity-research-writeup` → `position-exit-rules` → `equity-trade-decision` size and enter within them; `weekly-portfolio-review` checks drift against them; `portfolio-thesis-audit` grades each position on its own thesis.
- **Still recommended, not built:** `etf-selection` — **built 2026-09-09, item 29 below. That completes the three-skill post-doc shortlist.** `tax-aware-selling` still floated as lower priority / possible `weekly-portfolio-review` mode.
- Memory: `skill-added-asset-allocation-policy.md`.

### 29. `etf-selection` — Claude-recommended (3rd/last of the post-doc shortlist), user picked it next

- [x] Built `2026-09-09` — `.claude/skills/Finance/etf-selection/SKILL.md` (single-file). The funds counterpart to `equity-research-writeup`, filling the slot `asset-allocation-policy` defines. Three skills already punted fund-ticker evaluation with carve-outs that **pointed nowhere**: `equity-research-writeup` ("Not for ETFs or funds"), `portfolio-thesis-audit` ("autopilot index / target-date funds, no per-position thesis"), `asset-allocation-policy` ("not which specific fund fills a sleeve — SPY-vs-VOO is a separate question", a carve-out added during item 28 specifically anticipating this skill). The user holds SPY + sector + metals ETFs and a broad-index Roth.
- **Gate.** Withholds a hold/buy/swap verdict until the user (a) assigns the fund an `asset-allocation-policy` sleeve and (b) pulls its real facts from the **issuer fact sheet / prospectus** — expense ratio, index + # holdings + top-10 weight + sector/geo, AUM + ADV (closure risk), multi-year tracking difference, structure (physical/synthetic, in-kind, domicile), distribution yield + ROC flag, fund age; **bond fund** → duration / credit / YTM; **commodity fund** → physical-vs-futures (roll drag) + tax treatment. Plus a **holdings-overlap %** against every adjacent holding (an 85%-same-names fund is a fee-heavy duplicate, not diversification), and for any non-core sector/factor/thematic fund a stated **why-this-tilt-why-now + decay check** (durable exposure vs. a narrow product launched near the hype). Verdicts: HOLD/BUY · SWAP (name the cheaper same-role fund) · PASS (no role / duplicate / thematic-no-thesis / bad cost or structure) · RESEARCH (facts not sourced). Claude structures + flags + verdicts; never fetches fund data, picks a theme, or sizes the sleeve.
- **Isolation screen: MIXED** (1 worktree agent, non-consulting, web access). On "hold SPY or switch to VOO/VTI, plus add an AI ETF and a cyber ETF", the baseline: required a falsifiable 3-sentence "why this tilt, why now" for the thematic funds (matching the skill), flagged overlap as the specific trap and gave a manual overlap procedure, produced a hold/buy/swap verdict table, and flagged its own figures as memory-sourced with a staleness caveat. But it **answered SPY-vs-VOO from memory** (~0.09% vs ~0.03%, "$6/yr on $10k") rather than gating on the fact sheets — the exact thing the skill's pressure clause targets — **did not compute the overlap** ("exact figures would be guesses"), **skipped the structured sourced-facts block** (no AUM/volume/closure-risk/tracking-difference/structure fields), and gave a direct SPY recommendation without the sleeve/role framing. So the skill's value: the sourced-facts gate (not answering the well-known ER gap from memory), the structured evaluation block, and overlap as a *required computed input* rather than advice. Recorded MIXED — the baseline had genuinely good instincts (thesis requirement, overlap-as-trap, verdicts, staleness flags), consistent with this whole batch of competent "help me pick X" baselines.
- `skill-interaction-testing` **CLEAN after fixes, 13 scenarios, ~10 edits** (1 worktree agent; skill committed first). Pool: tier 1 all nine other `Finance/` skills; tier 2 `ambiguity-gate`, `learning-gate`. **One real Starvation found and fixed (scenario 4):** a held **thematic** ETF fell between `portfolio-thesis-audit`'s exclusion (which only named "index / target-date funds") and its scope — it would have run a stock-style thesis audit on a clean-energy ETF. Fixed: `portfolio-thesis-audit` description clause + a new out-of-scope bullet routing a held sector/factor/thematic fund's tilt thesis to `etf-selection`. Everything else was the standard **zero inbound pointers** pattern (the new skill's outward pointers were all good). Fixes:
  1. `equity-research-writeup` — the "Not for ETFs" description clause + body bullet now name `etf-selection` (funds counterpart).
  2. `asset-allocation-policy` — the "which specific fund fills a sleeve" bullet now names `etf-selection`.
  3. `portfolio-thesis-audit` — the starvation fix above (description + body bullet).
  4. `learning-gate` — Step 3 row + don't-stack enumeration.
  5. `weekly-portfolio-review` — step 1 + the description route list + the output block now route a held fund's broken tilt thesis / a cheaper equivalent / grown fund-to-fund overlap to `etf-selection` (it previously had no route for *any* fund-level finding despite being the integration pass).
  6. `equity-trade-decision` — a "What this does not do" bullet: for an ETF purchase the fund is vetted in `etf-selection` (satisfies checklist item 1), the cycle-stage sector-tilt table doesn't apply to a broad basket, risk-budget sizing still runs. `etf-selection` gets the reciprocal feed-forward (BUY/HOLD → `equity-trade-decision` for sizing).
  7. `watchlist-screener-criteria` — a carve-out: an ETF has no ROIC/margins to screen on → `etf-selection`.
  8. This skill — the sibling-standard "'just tell me if VOO beats SPY' is a reason to want the gate skipped, not a release of it" pressure line.
- Pairs recorded tested-and-clean (`etf-selection` ×): `asset-allocation-policy`, `equity-research-writeup`, `portfolio-thesis-audit`, `watchlist-screener-criteria`, `equity-trade-decision`, `position-exit-rules`, `covered-call-decision`, `weekly-portfolio-review`, `seller-financing-evaluation`, `learning-gate`, `ambiguity-gate`.
- Group: `Finance/` (10th skill). **Completes the post-doc three-skill shortlist** (`equity-research-writeup` 27, `asset-allocation-policy` 28, `etf-selection` 29). The `Finance/` equity system is now: `asset-allocation-policy` (the frame) · `watchlist-screener-criteria` → `equity-research-writeup` **/ `etf-selection`** → `position-exit-rules` → `equity-trade-decision` (screen/research/exits/size, individual names *or* funds) · `covered-call-decision` (income) · `weekly-portfolio-review` (weekly pass, routes to all) · `portfolio-thesis-audit` (per-position keep/sell).
- **Still floated, not built:** `tax-aware-selling` (lot selection, wash-sale window, LT/ST, low-income-year LTCG harvesting) — lower priority, possibly a `weekly-portfolio-review` mode rather than standalone.
- Memory: `skill-added-etf-selection.md`.

### 30. `incremental-build-pacing` — added outside the workflow, reconciled by the 2026-09-09 drift audit

- [x] On disk since commits `7e0b0bd` / `4a41f06` (user's own "added and updated skill" commits, before the 2026-09-09 Finance batch). Full `SKILL.md` + `README.md` + `increment-delivery.md`. **A gate** (delivery cadence): for an already-planned build the user wants to follow as it's assembled, withholds the one-turn batch, sets an increment size + checkpoint cadence, delivers one piece at a time with a comprehension check before each advance. Governs cadence, not scope — `spec-drift-gate` / `design-scoping` settle the slice first; `learning-gate` Step 3 routes here.
- **Bookkeeping status:** wired into the catalog — referenced by `learning-gate` (Step 3 row + `guided-walkthrough.md` + the don't-stack paragraph), `spec-drift-gate`, `problem-solving-gates`, `tech-decision-walkthrough`, `system-design-communication`. Was missing from `README.md` (added by this audit — Skill Development row) and from this backlog (this entry). No memory file, and **no recorded isolation screen or `skill-interaction-testing` run** — FLAGGED for a retroactive interaction test if revisited (hand to `skill-interaction-testing` Step 2 onward; likely pool: `learning-gate`, `spec-drift-gate`, `design-scoping`, `guided-walkthrough.md` case, `problem-solving-gates`).
- Group: `Skill Development/`. Source: a `how-to` fold thread + the `_Other/` triage lineage (see item 27-era notes / `skill-folds-other-directory.md`), plus fresh authoring — not fully traced.

### 31. `tech-decision-walkthrough` — added outside the workflow, reconciled by the 2026-09-09 drift audit

- [x] On disk since commits `7e0b0bd` / `4a41f06` (same as item 30). Full `SKILL.md` + `README.md` + `decision-loop.md`. **A procedure**, not a gate: coached conversational walk through a build's technology choices one decision at a time (interview-style), each ending in a recommendation + the user's call + a short ADR; depth scales with blast radius, load-bearing decisions route into the specialist Architecture skill. Downstream of `design-scoping`, upstream of `spec-drift-gate` → `incremental-build-pacing`. Two registers (collaborative / interviewer) set by `learning-gate`'s assistance level.
- **Bookkeeping status:** heavily wired — referenced by `learning-gate` (Step 3 row + line-59 clause + don't-stack paragraph), `design-scoping`, `problem-solving-gates`, `system-design-communication`, `api-interface-style`, `technical-cost-decision`, and `watchlist-screener-criteria` (a carve-out added during item 25). Was missing from `README.md` (added by this audit — Architecture row) and this backlog (this entry). No memory file, **no recorded isolation screen or `skill-interaction-testing` run** — FLAGGED for a retroactive interaction test if revisited (likely pool: `design-scoping`, `problem-solving-gates` Options Generator, `system-design-communication` Mode 3, `incremental-build-pacing`, the specialist Architecture skills, `technical-cost-decision`).
- Group: `Architecture/`. Source: fresh authoring around the `learning-gate` "walk me through the stack" Step 3 thread — not fully traced.

### 32. `debt-credit-management` — user-requested, from a gap-analysis pass over the whole `Finance/` group

- [x] Built `2026-09-11` — `.claude/skills/Finance/debt-credit-management/SKILL.md` (single-file, matches the group). Closes a genuine catalog gap surfaced when the user asked which of five candidate gaps (account-control, debt/credit, cash-sleeve purpose, tax-lot mechanics, contribution sequencing) were real vs. already covered — confirmed by reading the actual files, not assumed: `asset-allocation-policy`'s capital map assumes money to invest, never money owed, and nothing else in `Finance/` touches liabilities at all. A gate: no close/consolidate/keep or payoff-order verdict on a credit card until the user supplies, per card, APR, balance, limit, annual fee, rewards category, and account age, plus **combined utilization across every card** (total balances ÷ total limits — the number scoring models actually weight, not any single card's ratio) and one named goal (pay down fastest / protect score / simplify). Forces the before/after combined-utilization and average-account-age effect of any proposed closure — the specific trap (closing a card can *raise* utilization and shorten history, hurting the score the "cleanup" was meant to protect) that motivated the skill. Revolving credit only; installment debt (mortgage/auto/student loans) is named out of scope. Description written tight from the start this session (984 chars) as a `description: |-` YAML block scalar rather than the catalog's usual unquoted plain-scalar form — both to fit the 1024-char cap for uploading to Claude and because the plain form (used by every other Finance skill) fails strict YAML parsing on its own `: ` characters, confirmed against all 10 existing Finance skills.
- **Isolation screen: MIXED** (1 worktree agent, non-consulting). On "I have 11 credit cards with different balances and limits, some I never use — help me figure out which ones to close," the baseline already knows qualitatively that closing cards can hurt utilization and account-history length (common enough to be volunteered unprompted) — but does not reliably force an exhaustive per-card table, compute an actual numeric combined-utilization ratio, require one named goal before screening a candidate, or produce a numeric before/after per closure candidate. Recorded honestly: the skill's value is method/completeness/trackability, not fixing an outright baseline failure.
- `skill-interaction-testing` **CLEAN on 14 scenarios, 3 fixes, all one-directional** (1 worktree agent; skill committed to the branch first). Pool: all nine other `Finance/` skills, `ambiguity-gate`, `learning-gate`, `problem-solving-gates` (confirmed software-only from its own description, so a "why do I keep carrying a balance" framing stays this skill's, not a debugging session), `spec-drift-gate`. Clean pairs confirmed: parallel non-bleeding hand-off with `seller-financing-evaluation` (revolving vs. seller-note debt), correct self-exclusion to `learning-gate` on bare concept questions, clean multi-turn gate release, and correctly-named-and-stopped boundaries (balance-transfer mechanics, credit-report disputes, installment debt) with no other skill picking those up either. Fixes — all completing a pointer this skill already started, none touching `debt-credit-management` itself:
  1. `asset-allocation-policy` — "What this does not do" gained a bullet naming the capital-routing tension (a high-APR balance competing with the runway/contribution target for the next dollar) and pointing to this skill, since its capital map previously had zero mention of revolving debt.
  2. `ambiguity-gate` — Step 1 table gained a row: a settled "clean up / decide what to do with my credit cards" ask hands off whole, since this skill runs its own goal-naming gate (same precedent as the `portfolio-thesis-audit` row above it).
  3. `learning-gate` — Step 3 table gained a row (every other `Finance/` skill already had one) plus a mention in the don't-stack enumeration, so a live card-numbers-bearing but debug-shaped prompt doesn't get `learning-gate`'s own framing question stacked on top of this skill's gate.
- Also flagged (not fixed, `catalog-drift-audit` territory): `skill-interaction-testing`'s own Step 1 text names `problem-journal` as one of the four cross-cutting tier-2 skills, while `skill-architecture.md`'s rule table names `spec-drift-gate` instead — the two in-repo sources disagree on what the fixed four are.
- Reciprocal edit beyond the interaction test: `etf-selection` and `portfolio-thesis-audit` both gained a line pointing at `asset-allocation-policy`'s new account-control field (see item below) — a robo- or employer-managed account's specific holding is the provider's pick, not the user's, and grading it as a thesis/overlap choice is the wrong frame. Found via the same gap-analysis pass, not this skill's own interaction test.
- Group: `Finance/` (11th skill).
- Source: user's own gap-analysis of the `Finance/` group (this session), not a vault note.

### 33. `tax-lot-selection` — user-requested, 2nd of the same gap-analysis pass

- [x] Built `2026-09-11` — `.claude/skills/Finance/tax-lot-selection/SKILL.md` (single-file). Closes the tax-lot gap `position-exit-rules` explicitly punts ("Tax lots and sell mechanics... this skill defines when the rule triggers, not how the order is placed") and the same gap exists, unstated, in `portfolio-thesis-audit`, `weekly-portfolio-review`'s trim flag, and `covered-call-decision`'s assignment. A gate that starts **only once a sell is already decided** by one of those four — it never decides whether/when to sell, only which shares. Withholds a lot answer until every lot (acquisition date, quantity, cost basis) is on the table and one goal is named (minimize this year's tax / avoid triggering short-term treatment / confirm the broker's default). Computes each lot's holding period and gain/loss, cross-checks the selection against what the broker's stated default method (FIFO / average-cost / an existing specific-ID election) would actually pick, and — before confirming any loss sale — runs a wash-sale check across the 61-day window, explicitly including automatic dividend reinvestment as a common invisible trigger. Taxable accounts only; a Roth/IRA/HSA sale has no cost basis or gain/loss to track, named and skipped. Description written tight from the start (987 chars, `description: |-` block scalar) same as `debt-credit-management`.
- **Isolation screen: PASS** (1 worktree agent, non-consulting). On "`position-exit-rules` just triggered my stop on 50 shares of NOBL, 3 lots — which shares do I actually sell", the baseline plausibly asks about cost basis (well-known enough) but is unlikely to build a full comparative per-lot table (not just the lot it ends up recommending), force one named goal before picking, cross-check against the broker's actual default method, or reliably probe the DRIP-as-invisible-wash-sale-trigger detail. Real, non-manufactured gaps, recorded as a clean PASS rather than inflated.
- `skill-interaction-testing` **CLEAN on 16 scenarios, 4 findings, all one-directional** (1 worktree agent; skill committed to the branch first). Pool: all ten other `Finance/` skills (full read for the four upstream sell-producing skills + `debt-credit-management`, description-level for the rest), `ambiguity-gate`, `learning-gate`, `problem-solving-gates`, `problem-journal`, `spec-drift-gate` (tested against both candidate "fixed four" lists — see the flagged discrepancy below). Clean pairs confirmed: hand-off from each of `position-exit-rules` / `portfolio-thesis-audit` / `weekly-portfolio-review` / a `covered-call-decision` assignment, correct Roth/IRA/HSA self-exclusion, the DRIP wash-sale trigger firing as designed, a correctly-silent gain-only sale (no wash-sale math run when nothing is a loss), clean multi-turn gate release, and no collision with `problem-journal` or `spec-drift-gate`. Also surfaced a genuinely useful chain (not a fix): `portfolio-thesis-audit`'s EXIT + disposition-effect flag on a losing position feeding directly into this skill's loss-harvest goal and wash-sale check. Fixes:
  1. `learning-gate` — Step 3 table gained a row (every other sell-adjacent Finance skill already had one) plus a mention in the don't-stack enumeration, closing a real starvation risk: a live, position-specific FIFO-vs-specific-ID question could otherwise get answered as a bare Reference/Learning question, skipping the gate.
  2. `covered-call-decision` — its "Assignment outcome" block computed a single realized-gain figure with no acknowledgment that assigned shares spanning more than one tax lot need this skill to pick which lot(s) actually get called; added a line naming that hand-off (the reverse pointer already existed on this skill's own side).
  3. `tax-lot-selection` itself — the "sell decision's source" phrasing (precondition item 3 + a red flag) read as a closed list; loosened to "any other already-fixed decision with a real share count," and named the debt-payoff-driven-sale case (`debt-credit-management`/`asset-allocation-policy` name that tension but don't resolve it) as a real source this skill should still accept.
- Also flagged, not fixed (`catalog-drift-audit` territory, independently reconfirmed this run): `skill-architecture.md`'s "four cross-cutting meta-skills" list (`learning-gate`/`problem-solving-gates`/`spec-drift-gate`/`ambiguity-gate`) still disagrees with `skill-interaction-testing/SKILL.md`'s own Step 1 fixed list (`ambiguity-gate`/`learning-gate`/`problem-solving-gates`/`problem-journal`) — same discrepancy first surfaced building item 32 above, not yet reconciled.
- Group: `Finance/` (12th skill).
- Source: user's own gap-analysis of the `Finance/` group (same session as item 32), not a vault note.

### 34. `retirement-contribution-sequencing` — user-requested, 3rd/last of the same gap-analysis pass

- [x] Built `2026-09-11` — `.claude/skills/Finance/retirement-contribution-sequencing/SKILL.md` (single-file). Closes the gap `asset-allocation-policy` explicitly named and punted twice over ("Optimise tax-advantaged contributions... Backdoor Roth mechanics, capturing an employer match, the order to fund accounts — separate territory"). A gate on *which account* (401(k), HSA, Roth — direct or backdoor, rollover/traditional IRA, taxable) gets new retirement dollars first, upstream of `asset-allocation-policy`'s contribution routing (which picks the asset class once the account is chosen). Withholds a funding-order answer until every account's contributions-so-far and limit-remaining is on the table, the employer match formula and whether it's fully captured is stated (an unclaimed match outranks everything else — a guaranteed-return loss no market beats), HSA eligibility is confirmed against actual HDHP enrollment rather than assumed, and — when income is near the Roth MAGI phase-out — the user's existing pre-tax IRA balance is checked for the backdoor-Roth pro-rata trap (a real, easy-to-miss gotcha: converting to Roth while pre-tax IRA money exists anywhere, including an old 401(k) rollover, makes part of *every* future conversion taxable). Applies the standard match → HSA → Roth → rest-of-401(k) → taxable hierarchy against real numbers, never recited generically. Names, doesn't resolve, the competing claim from `debt-credit-management`. Description written tight from the start (975 chars, `description: |-` block scalar), same approach as the other two skills from this pass.
- **Isolation screen: PASS** (1 worktree agent, non-consulting). On a multi-account prompt (401(k) w/ match + HSA/HDHP + Roth IRA + old rollover IRA + $500/month extra), the baseline fluently recites the standard match→HSA→Roth→401k→taxable hierarchy — that ordering knowledge itself isn't new — but doesn't reliably verify exact limits-remaining per account, doesn't probe actual HDHP eligibility rather than taking it at face value, doesn't check the Roth MAGI phase-out against real income, and critically is unlikely to ever connect the mentioned rollover IRA to a pro-rata problem unless the user explicitly says "backdoor Roth." The skill's account-specific verification discipline — and structurally surfacing the pro-rata trap even when the user never uses that phrase — is real, non-cosmetic value recorded honestly against a fluent-but-unverified baseline.
- `skill-interaction-testing` **CLEAN on 16 scenarios (14 prompts + 2 reasoned cross-cutting checks), 6 findings, all one-directional, 0 reciprocal** (1 worktree agent; skill committed to the branch first). Pool: all 13 other `Finance/` skills (full read for `asset-allocation-policy` and `debt-credit-management`, description-level for the rest, plus a targeted check of `equity-trade-decision`/`etf-selection`'s own dead-end carve-outs), `ambiguity-gate`, `learning-gate`, `problem-solving-gates`, and — testing against both disputed "fixed four" lists rather than picking a side — both `spec-drift-gate` and `problem-journal` (neither collides with this skill; see the catalog fix below). Confirmed clean: the account-control field added building item 32 is correctly irrelevant to this skill (who manages picks *inside* an account doesn't matter for *which account* gets funded), multi-turn gate release with no re-fire on the "taxable" tail line, the pressure test holding per its own red-flags, the pro-rata check firing correctly on a direct "should I do a backdoor Roth" question, and a combined "set my allocation AND tell me where my contribution goes" request correctly using different per-account data in each skill rather than duplicating a precondition. Fixes:
  1. `asset-allocation-policy` — its own `"where should new contributions go"` trigger phrase (frontmatter description) risked claiming this skill's exact use case first; qualified in place ("which asset class... which account is `retirement-contribution-sequencing`").
  2. `asset-allocation-policy` — the "Optimise tax-advantaged contributions" carve-out (pointed nowhere since it was written, per item 28/33's own notes) now names this skill.
  3. `debt-credit-management` — its "retirement contribution" competing-claim clause was pointing at `asset-allocation-policy`, which itself disclaims that exact case (a 3-way dead loop found by the test); split off and repointed at this skill.
  4. `learning-gate` — Step 3 table gained a row (every other sell/contribution-adjacent Finance skill already had one) plus the don't-stack enumeration.
  5. `equity-trade-decision` — its vague "Backdoor Roth mechanics... separate territory" carve-out now names this skill for the parts it actually owns (Infinite Banking / death-benefit sizing remain named as genuinely unowned).
  6. `problem-journal` — its non-coding-mistake example list gained "I keep missing my employer match / messing up the funding order" → this skill.
- **Also fixed, not just flagged this time** (the standing catalog-drift item first surfaced building item 32, reconfirmed again building item 33): `Prompts/skill-interaction-testing/SKILL.md`'s own Step 1 fixed-cross-cutting list said `problem-journal`, contradicting `skill-architecture.md`'s canonical four (`learning-gate`/`problem-solving-gates`/`spec-drift-gate`/`ambiguity-gate`). Flagged a third time by this run's own reasoning (per the file's own inclusion criterion — shape-based trigger, not domain — `spec-drift-gate` fits it and `problem-journal` doesn't), so fixed directly rather than deferred again: swapped `problem-journal` out of the fixed tier-2 list for `spec-drift-gate`, and reclassified `problem-journal` as a tier-3 concept-match candidate to check per-skill (which is exactly how it was actually being used in practice across items 32-34 anyway).
- Group: `Finance/` (13th skill). **This closes all three confirmed-real gaps from the original 5-item gap-analysis** (item 1, account-control, was folded into `asset-allocation-policy` directly rather than built standalone — see the "Fold into existing skills" entry below item 31; item 3, cash-sleeve purpose, was already covered; items 2/4/5 are items 32/33/34).
- Source: user's own gap-analysis of the `Finance/` group (same session as items 32-33), not a vault note.

---

## Fold into existing skills — reference material, no new skill

- [x] Done `2026-09-11` — **`asset-allocation-policy`** account-control field. The capital map (precondition input 1) previously broke each account out by tax type only (taxable / Roth-IRA / 401(k)); it never asked *who controls the picks inside it*. Real gap surfaced by the user (misgrading a robo-managed Wealthfront Roth's fund overlap as the user's own decision, when the picks were the provider's). Added as a required sub-field of input 1: **self-directed** / **robo-managed** (e.g. Wealthfront) / **employer-managed** (a 401(k) target-date fund or fixed plan menu) per account, echoed in the policy block's CAPITAL MAP section and a new red flag ("an account's control type left unstated, so a robo- or employer-managed holding gets graded as if the user picked it"). Reciprocal one-line pointers added to `etf-selection` and `portfolio-thesis-audit` — check the control type before auditing or comparing a specific holding inside a robo- or employer-managed account. Not run through a full `skill-interaction-testing` pass (a field addition to an existing precondition, not a new skill); folded in as part of building `debt-credit-management` above.

- [x] Done `2026-09-05/06` — the imported `.claude/skills/_Other/` pile (8 skills the user copied from an external content-creator pack) triaged. **Folded (source deleted):** `my-viral-post` → **`software-carpentier-brand`** (new "Drafting a LinkedIn feed post" mode); `grill-me` → **`spec-drift-gate`** (new Step 2a scoped extraction interview + one-line-build trigger); `how-to` → **`learning-gate`** (new `guided-walkthrough.md` reference + Step 3 row). **Relocated:** `reddit-researcher` → new `Research/` group (rewritten to drop the paid Apify dependency); `Anti AI Writing Style` → `Business/delete-ai-words/` (folder renamed to match `name:`). **Deleted:** `linkedin-hook`, `Prompt optimizer` (concept rebuilt fresh as item 18), `negotiation`. `_Other/` folder removed. Interaction-tested per skill (2 + 1 worktree agents): 5 one-line routing carve-outs total across `spec-drift-gate`, `software-carpentier-brand`, `ambiguity-gate`. Memory: `skill-folds-other-directory.md`, `skill-added-prompt-authoring.md`.

- [x] Done `2026-09-04` — [Separate vs Combined Codebase Repositories](Architecture/Separate%20vs%20Combined%20Codebase%20Repositories.md) → **`microservices-decision`**. Was item 4 of the Estimation/FMEA/Scoping batch (`repo-topology`, priority 4, borderline); isolation-screened before enriching the note — a fresh worktree baseline (no skill) gave a solid, ADR-shaped, tooling-literate answer (named Nx/Turborepo/Bazel/CODEOWNERS unprompted) with only a soft gap (assumptions disclosed after the recommendation, not gated before it), unlike the other three batch items where baseline failed outright. Not gate-worthy as a standalone skill — folded instead: new "Repo layout follows from this" section in `SKILL.md` (repo-per-service / one repo / hybrid reads off the existing Readiness Block's `Independent deploy` + `Owner per service` fields; states the version-skew-vs-undifferentiated-CI trade explicitly) + new `repo-layout.md` reference (affected-graph CI tool comparison, `CODEOWNERS` mechanics, CI cost at scale, repo split/merge as a migration). Reciprocal one-line pointers added to `deployment-strategy`, `migration-cutover`, `technical-cost-decision` out-of-scope sections. `skill-interaction-testing` run (7 scenarios, 2 agents — both worktrees were stale relative to this branch, so runs were file-read desk-checks rather than live tool invocations, faithfully hand-applying each skill's contract): 2 hand-offs, 2 correctly-silent controls, 1 chaining (greenfield → design-scoping → back), 1 minor internal-field friction (fixed), 1 real contradiction (fixed) — `microservices-decision` pointed "pipeline shape" to `deployment-strategy`, which explicitly disclaims CI/pipeline mechanics; split into build/test-CI-shape (kept in-house, `repo-layout.md`) vs release-ordering/rollout (stays `deployment-strategy`). Also added an N/A-defaults note to the Readiness Block for repo-layout-only asks on already-existing services. Re-ran the failing scenario (non-isolated this time — the earlier worktree staleness was a git worktree only seeing committed history, missing these uncommitted edits entirely): confirmed fixed, no ping-pong, substantive answer reached; one cosmetic follow-up fixed (the CI-shape content lived as a bullet under "Out of scope" while claiming in-scope, folded into the adjacent bullet instead). Memory: `skill-interaction-repo-topology.md`.
- [x] Done `2026-09-04` — [File System Storage](Architecture/04.%20Backing%20Service%20Options/File%20System%20Storage.md) → **`database-architecture`**. New `store-options.md` (block vs object vs DFS; blob-in-object-storage vs DB-row decision table keyed on size / transactional-consistency / access pattern; large-binary patterns — uploads, exports, backups, static assets, regulated document stores; HIPAA/GDPR/PCI-on-object-storage controls). Wired into SKILL.md "When persistence isn't decided yet" + README Files table + "Built from". No trigger change.
- [x] Done `2026-09-04` — [12 Key Metrics for Measuring Service Performance](Architecture/12%20Key%20Metrics%20for%20Measuring%20Service%20Performance.md) → **`observability-strategy`**. Expanded the 5-metric sentence in `signals-and-slos.md` into a "Service-metric menu — candidate SLIs" table (all 10: TTFB, latency, response time, throughput, request rate, error rate, concurrent connections, cache hit ratio, MTBF/MTTR, SLA compliance) with method-bucket + SLI-use columns and the "trend > absolute" note; explicit out-of-scope for team-delivery metrics. `observability-framework.md` step 2 + README point at it.
- [x] Done `2026-09-04` — [SAGA](Architecture/SAGA.md) → **`data-tier-operations`**. Already covered — `consistency-and-transactions.md` has a full Saga subsection (orchestrated vs choreographed, compensating actions, guarantees/accepts/fits), expanded past the note. Added `SAGA.md` to the README "Built from" list for traceability. (Choreography/orchestration also feeds the event-driven loose thread below.)
- [x] Done `2026-09-04` — [Fault Tolerance](Architecture/Fault%20Tolerance.md) → **`resilience-strategy`**. Already a listed source; cross-check passed — `mechanisms-and-tradeoffs.md` covers retry budget + backoff/jitter, circuit breaker, bulkhead (= the note's isolation/containment), timeout, graceful degradation, more rigorously than the note. Added a "Not here: failover and replication" note routing infra/DB failover → `data-tier-operations` and monitoring/alerting → `observability-strategy`.
- [x] Done `2026-09-04` — [Monitoring & Observability](Architecture/Monitoring%20%26%20Observability.md) → **`observability-strategy`**. Already a listed source; cross-check passed — `signals-and-slos.md` has a named "Monitoring vs observability" section and covers logs/traces/metrics in more depth than the note. No change needed.
- [x] Done `2026-09-04` — [Cloud/AWS/Cloud Storage.md](Architecture/04.%20Backing%20Service%20Options/Cloud/AWS/Cloud%20Storage.md), [Cloud Databases.md](Architecture/04.%20Backing%20Service%20Options/Cloud/Cloud%20Databases.md), [Cloud/AWS/Cloud Storage & Databases.md](Architecture/02.%20Backing%20Service%20Options/Cloud/AWS/Cloud%20Storage%20%26%20Databases.md) → **`database-architecture`**. New "Concrete service names — AWS" section in `store-options.md` — storage-shape table (EBS/S3/EFS/Storage Gateway/Snowball mapped to block/object/DFS/hybrid-bridge/bulk-transfer) and a database-paradigm table (the 8 paradigms → Aurora/RDS, DynamoDB, ElastiCache, DocumentDB, Keyspaces, Neptune, Timestream, QLDB) + the IaaS-vs-managed axis note. SKILL.md's paradigm line and the README point at it. `AWS Data Analyst Services.md` / `AWS Processing Services.md` (Athena/QuickSight/CloudSearch/Glue/EMR/Data Pipeline) reviewed and NOT folded — pure ETL/BI tool catalog with no decision to gate; no skill in this catalog owns "which analytics/ETL tool," and the ones named would only ever surface as vocabulary inside a `dimensional-modeling` conversation if ever needed.
- [x] Done `2026-09-04` — [Cloud/AWS/AppSync.md](Architecture/04.%20Backing%20Service%20Options/Cloud/AWS/AppSync.md) → **`api-interface-style`**. New "Managed implementations" paragraph in `style-tradeoffs.md`'s GraphQL section — AppSync/Hasura trade the resolver/cost-limiter build-out for a managed service and typically add subscriptions + offline sync, but don't remove GraphQL's fundamental costs. `Req and RES.md` (generic request/response contract-discipline advice) reviewed — too thin/generic to add here (the API-boundary-contract point it makes is already implicit in this skill); its one concrete, AWS-specific nugget (Step Functions' JSONPath data flow between states) went to `serverless-execution-model` instead (see below).
- [x] Done `2026-09-04` — [Cloud/AWS/AWS CI-CD Pipeline.md](Architecture/04.%20Backing%20Service%20Options/Cloud/AWS/AWS%20CI-CD%20Pipeline.md) → **`deployment-strategy`**. New "Vendor mechanics — where each pattern lives on ECS vs. Kubernetes (EKS)" section in `rollout-patterns.md` — CodeDeploy natively supports rolling/blue-green/canary on ECS; vanilla Kubernetes only gives rolling natively, blue-green/canary need Argo Rollouts or Flagger. Names the "we're on EKS and want canary" under-specification trap.
- [x] Done `2026-09-04` — [Step Function Cost.md](Architecture/04.%20Backing%20Service%20Options/Cloud/Step%20Function%20Cost.md) → **`technical-cost-decision`**. New "The billing unit is not always 'per request'" paragraph after the Cost Surface template — Step Functions Standard bills per-1,000-state-transitions (not per execution), so state count is itself a cost lever (retries and Map/Parallel iterations all count); Express workflows bill per-invocation + GB-seconds instead, closer to Lambda's model.
- [x] Done `2026-09-04` — [LocalStack Setup.md](Architecture/04.%20Backing%20Service%20Options/Cloud/LocalStack%20Setup.md), [LocalStack Data Persistence.md](Architecture/04.%20Backing%20Service%20Options/Cloud/LocalStack%20Data%20Persistence.md), [Containerizing LocalStack Environment.md](Architecture/04.%20Backing%20Service%20Options/Cloud/Containerizing%20LocalStack%20Environment.md), [Testing Step Functions.md](Architecture/04.%20Backing%20Service%20Options/Cloud/Testing%20Step%20Functions.md) → **`test-strategy`**. Two new paragraphs in `test-levels.md`'s Integration section — cloud-service emulation (LocalStack) as the "real dependency" for a managed-cloud stack, with the ephemeral-vs-persisted-state gotcha and the "still simplified vs. the real service" caveat; and how the unit/integration/E2E three-tier split applies to a poorly-testable artifact like a Step Functions state machine (structural validation / emulator execution / staging).
- [x] Done `2026-09-04` (second pass, see item 5 above) — messaging-technology fold-in gap closed: `serverless-execution-model` was missing the actual SQS/SNS/Kinesis/Kafka topology *choice* (only had the Lambda-consumption side). Added.

---

## No skill / no action

- [Impact of Architectural Styles](Architecture/02.%20System%20Design/Impact%20of%20Architectural%20Styles.md) — survey with a self-admitted non-standard taxonomy; a "pick your architectural style" skill would silently override `microservices-decision` / `api-interface-style` / `data-tier-operations`. Points at the two loose threads below.
- [HLD vs LLD](Architecture/HLD%20vs%20LLD.md) — textbook HLD/LLD definitions (city-map vs building-blueprint analogy) + a differences table. Pure vocabulary, no procedure and no decision. A "pick your design altitude" skill would silently front-run `microservices-decision` / `api-interface-style` / `data-tier-operations` (same failure mode as Impact of Architectural Styles). The one reusable idea in its companion note lands in `design-scoping` (see item 3).
- [Reactive programming](Architecture/Reactive%20programming.md) — code-level paradigm (async streams, Observables, Rx), wrong altitude for an architecture skill. Belongs to the JS Reactive Programming ecosystem note.
- [Agile.md](Communication/Companies%20Worked%20At/TD/Agile.md) — Scrum/Kanban/backlog vocabulary survey, no decision to gate.
- [PI Planning.md](Communication/Companies%20Worked%20At/TD/PI%20Planning.md) — mostly TD-specific fiscal-quarter/PI-cadence mechanics; the one reusable idea (shirt-size sizing heuristic) was stripped of TD specifics and folded into `user-story-decomposition` (item 6 above).
- [Current State Assessment.md](Communication/Companies%20Worked%20At/TD/Current%20State%20Assessment.md) — an org/workstream assessment questionnaire template; business-consulting altitude, not a requirements-authoring decision.
- [Building Risk.md](Communication/Companies%20Worked%20At/TD/Building%20Risk.md) — generic upskilling-program risk template, unrelated to requirements or user stories despite the folder.
- [TD Bank Lesson.md](Communication/Companies%20Worked%20At/TD/TD%20Bank%20Lesson.md), [Non-calendar fiscal year.md](Communication/Companies%20Worked%20At/TD/Non-calendar%20fiscal%20year.md) — career/company trivia, no decision content.

---

## Loose threads — evaluate separately

- [ ] **Requirements elicitation & lifecycle governance** — a BSA-conversation-technique and requirements-governance cluster found while reviewing `Communication/Companies Worked At/TD/` for `user-story-decomposition` (item 6 above), deliberately NOT folded into that skill (different job: it authors a stated feature into stories; this would be about producing/maintaining the requirement in the first place). Candidate sources: [Elicitation.md](Communication/Companies%20Worked%20At/TD/Elicitation.md) (question-ordering technique — What/Why/Which/How/When/Where, SMART, meeting structure) + [Type of Question.md](Communication/Companies%20Worked%20At/TD/Type%20of%20Question.md) (question-type taxonomy feeding it) + [Business Requirements Life cycle.md](Communication/Companies%20Worked%20At/TD/Business%20Requirements%20Life%20cycle.md) (stakeholder needs/wants/data analysis, RACI, conflict-resolution techniques — multivoting/weighted-ranking/Delphi, the Identification→Definition→Verification→Validation→Approval→Implementation lifecycle) + [Requirement Governance.md](Communication/Companies%20Worked%20At/TD/Requirement%20Governance.md) (traditional vs. adaptive change control) + [Validating.md](Communication/Companies%20Worked%20At/TD/Validating.md) (ongoing validation practice — walkthroughs, peer review, deep inspections). Likely shape: a procedure skill (like `document-page-check`/`failure-mode-analysis`), not a gate — "how to run the requirements conversation and keep requirements valid over the project," distinct from and upstream of `user-story-decomposition`'s "how to turn a stated requirement into stories." Would need its own boundary check against `design-scoping` (stakeholder/constraint overlap at a more general-business altitude vs. design-scoping's system-design-specific framing).
- [ ] **Event-driven architecture pattern choice** (event sourcing, choreography vs orchestration, system-wide EDA). Evaluate [Event-driven Architectural Pattern Decisions](Architecture/Event-driven%20Architectural%20Pattern%20Decisions.md) + [Event Driven Architecture](Architecture/Event%20Driven%20Architecture.md) as the sources. SAGA's choreography/orchestration content also feeds this.
- [ ] **Application-internal structure** gate (hexagonal / clean / layered / onion / DDD) — real catalog gap (nothing covers "how to structure the inside of one service"), but no written note yet (only a `#todo` link in Impact of Architectural Styles). Write the note first, drawing on the Clean Architecture book notes.
- **Chaos / fault injection** — not its own skill: target list → `failure-mode-analysis`, in-the-mix decision → `test-strategy`.
